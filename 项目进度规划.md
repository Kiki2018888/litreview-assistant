# ResearchAssistant 项目进度规划

> 版本：v1.0  
> 生成日期：2026-06-04  
> 总工期预估：6-8 周（含 2 周缓冲）  
> 开发原则：先查后改 · 先后端再前端 · 独立模块 · 字段前瞻性

---

## 一、总体路线图

```
Week 1-2  │ Step 1：后端骨架（核心链路闭环）
Week 3-4  │ Step 2：前端骨架（Electron + React 核心交互）
Week 5    │ Step 3：论文撰写模块
Week 6    │ Step 4：打包与发布 + 集成测试
Week 7-8  │ 缓冲期：Bug 修复、性能优化、文档完善
```

---

## 二、Step 1：后端骨架（Python FastAPI）

**目标**：打通"上传 PDF → 解析 → 提取摘要 → 问答"核心链路  
**验收标准**：Postman 完成完整流程；`/docs` 可访问；所有表存在；SSE 流正常；孤儿进程无残留。

### M1 项目脚手架（Day 1-2）

| # | 开发单元 | 工期 | 前置 | 状态 |
|---|----------|------|------|------|
| 1.1 | 目录结构与依赖管理（`requirements.txt`、`backend/` 目录） | 0.5d | 无 | ⬜ |
| 1.2 | `config.py` 配置中心（API Key / 模型名 / 路径 / 超时） | 0.5d | 1.1 | ⬜ |
| 1.3 | `main.py` 应用入口与生命周期管理 | 0.5d | 1.2 | ⬜ |

### M2 数据层（Day 2-4）

| # | 开发单元 | 工期 | 前置 | 状态 |
|---|----------|------|------|------|
| 1.4 | SQLAlchemy 2.0 模型定义（`backend/models/tables.py`） | 1d | 1.1 | ⬜ |
| 1.5 | Alembic 迁移脚本（基线版本，含所有表） | 0.5d | 1.4 | ⬜ |
| 1.6 | FTS5 虚拟表 `papers_fts` 与同步触发器 | 0.5d | 1.4 | ⬜ |
| 1.7 | `db.py` 引擎、会话管理与连接池 | 0.5d | 1.4 | ⬜ |

**涉及表**：`papers` / `paper_pages` / `extracted_data` / `paper_blocks` / `batches` / `paper_tags` / `chat_sessions` / `paper_audit_logs` / `settings` / `papers_fts`

### M3 基础服务（Day 4-6）

| # | 开发单元 | 工期 | 前置 | 状态 |
|---|----------|------|------|------|
| 1.8 | `pdf_parser.py` 文本提取（pdfplumber + pymupdf fallback） | 1d | 1.1 | ⬜ |
| 1.9 | `kimi_client.py` API 封装 + SSE 流式支持 | 1d | 1.2 | ⬜ |
| 1.10 | `extract_prompt.py` Prompt 模板（结构化提取 / 跨文献问答 / 单篇精读） | 0.5d | 1.9 | ⬜ |

### M4 文献 CRUD（Day 6-9）

| # | 开发单元 | 工期 | 前置 | 状态 |
|---|----------|------|------|------|
| 1.11 | `POST /upload` 批量上传 PDF（multipart，支持批次） | 1d | M2, M3 | ⬜ |
| 1.12 | `POST /{id}/parse` 手动触发解析 | 0.5d | 1.11 | ⬜ |
| 1.13 | `GET /` 文献列表 + 筛选（tag/year/status）+ 排序 | 1d | 1.12 | ⬜ |
| 1.14 | `GET /{id}` 单篇详情 + `GET /{id}/pages` 全文 | 0.5d | 1.13 | ⬜ |
| 1.15 | `PUT /{id}/tags` 标签更新 + `GET /tags` 标签汇总 | 0.5d | 1.13 | ⬜ |
| 1.16 | `DELETE /{id}` 级联删除（DB + PDF + 摘要） | 0.5d | 1.13 | ⬜ |

### M5 核心 AI 链路（Day 9-14）

| # | 开发单元 | 工期 | 前置 | 状态 |
|---|----------|------|------|------|
| 1.17 | `POST /{id}/extract` 单篇摘要提取（SSE，JSON Mode） | 1.5d | M3, M4 | ⬜ |
| 1.18 | `POST /batches/batch-extract` 批量提取（SSE 进度流） | 1.5d | 1.17 | ⬜ |
| 1.19 | `POST /chat` 跨文献问答（SSE，支持 batch_id） | 1.5d | 1.18 | ⬜ |
| 1.20 | `POST /{id}/chat` 单篇精读问答（SSE，use_fulltext 开关） | 1d | 1.19 | ⬜ |

### M6 辅助模块（Day 14-16）

| # | 开发单元 | 工期 | 前置 | 状态 |
|---|----------|------|------|------|
| 1.21 | `POST /{id}/translate` 摘要翻译 | 0.5d | 1.17 | ⬜ |
| 1.22 | 批次管理 API（`batches.py`：CRUD） | 0.5d | M2 | ⬜ |
| 1.23 | 设置 API（`settings.py` + keyring 加密存储） | 1d | 1.2 | ⬜ |
| 1.24 | 数据管理 API（`data.py`，仅限 localhost） | 0.5d | M2 | ⬜ |
| 1.25 | 会话历史 API（`chat_sessions.py`：CRUD） | 0.5d | M5 | ⬜ |

### M7 后端验收（Day 16-18）

| # | 开发单元 | 工期 | 前置 | 状态 |
|---|----------|------|------|------|
| 1.26 | pytest 单元测试（覆盖核心服务 + API） | 1d | M1-M6 | ⬜ |
| 1.27 | Postman 集成测试（上传→解析→提取→问答完整链路） | 1d | 1.26 | ⬜ |

---

## 三、Step 2：前端骨架（React + Electron）

**目标**：文献库 UI 和核心交互闭环  
**验收标准**：拖拽上传 5 篇 → 自动提取 → 列表展示 → 单篇问答 → 跨文献问答；SSE 正常；关闭无孤儿进程。

### M8 Electron 壳（Day 18-20）

| # | 开发单元 | 工期 | 前置 | 状态 |
|---|----------|------|------|------|
| 2.1 | Electron 主进程（spawn 后端 / health 轮询 / 端口探测 / kill） | 1d | 无 | ⬜ |
| 2.2 | `preload.js` IPC 安全暴露 | 0.5d | 2.1 | ⬜ |
| 2.3 | 打包配置（`package.json` + `electron-builder` 初配） | 0.5d | 2.1 | ⬜ |

### M9 React 骨架（Day 20-22）

| # | 开发单元 | 工期 | 前置 | 状态 |
|---|----------|------|------|------|
| 2.4 | 项目脚手架（Vite + React 18 + TS + Tailwind） | 0.5d | 无 | ⬜ |
| 2.5 | shadcn/ui 初始化 + 主题配置（light/dark/system） | 0.5d | 2.4 | ⬜ |
| 2.6 | 路由与布局（`Literature` / `PaperWrite` / `Settings`） | 0.5d | 2.5 | ⬜ |
| 2.7 | `api/client.ts` HTTP 客户端封装 | 0.5d | 2.4 | ⬜ |

### M10 核心组件（Day 22-28）

| # | 开发单元 | 工期 | 前置 | 状态 |
|---|----------|------|------|------|
| 2.8 | `FileUploader.tsx` 拖拽上传 + 多选（Ctrl/Cmd） | 1d | M9 | ⬜ |
| 2.9 | `LiteratureList.tsx` 列表 + 筛选 + 排序 + FTS5 搜索 | 1.5d | 2.8 | ⬜ |
| 2.10 | `LiteratureDetail.tsx` 单篇详情面板 | 1d | 2.9 | ⬜ |
| 2.11 | `PdfViewer.tsx` PDF 预览（react-pdf / iframe） | 1d | 2.10 | ⬜ |
| 2.12 | `ChatPanel.tsx` AI 对话面板（SSE 逐字渲染） | 1.5d | 2.11 | ⬜ |
| 2.13 | `MultiSelectBar.tsx` 跨文献多选工具栏 | 0.5d | 2.12 | ⬜ |
| 2.14 | `useSSE.ts` Hook（fetch + ReadableStream + 断线重连） | 1.5d | M9 | ⬜ |

### M11 功能页面（Day 28-31）

| # | 开发单元 | 工期 | 前置 | 状态 |
|---|----------|------|------|------|
| 2.15 | 文献库页面集成（上传→列表→详情→问答闭环） | 1.5d | M10 | ⬜ |
| 2.16 | 设置页（API Key / 模型选择 / 温度 / 测试连接） | 1d | 2.15 | ⬜ |
| 2.17 | 会话历史页面（查看 / 继续 / 删除） | 1d | 2.16 | ⬜ |

### M12 前端验收（Day 31-32）

| # | 开发单元 | 工期 | 前置 | 状态 |
|---|----------|------|------|------|
| 2.18 | 端到端测试（5 篇 PDF 全流程验证） | 1d | M11 | ⬜ |
| 2.19 | 关闭测试（进程列表验证，无 Python 孤儿进程） | 0.5d | 2.18 | ⬜ |

---

## 四、Step 3：论文撰写模块

**目标**：分块 Markdown 编辑器 + 学术润色  
**验收标准**：五个分块正常输入/预览/保存；润色返回学术化改写；自动保存 3 秒延迟。

| # | 开发单元 | 工期 | 前置 | 状态 |
|---|----------|------|------|------|
| 3.1 | `MarkdownEditor.tsx` 五标签页（摘要/引言/方法/结果/讨论）+ 实时预览 | 1.5d | M9 | ⬜ |
| 3.2 | 自动保存（停止输入 3 秒后 `PUT /blocks`） | 0.5d | 3.1 | ⬜ |
| 3.3 | `PaperWrite.tsx` 页面集成 | 0.5d | 3.2 | ⬜ |
| 3.4 | 润色功能：选中段落 → 调用 `/polish` → 对比查看 / 接受 / 拒绝 | 1.5d | 3.3 | ⬜ |
| 3.5 | 参考文献格式化（P2，可选） | 1d | 3.3 | ⬜ |

---

## 五、Step 4：打包与发布

**目标**：生成可安装包，推 GitHub 开源  
**验收标准**：Windows `.exe` / macOS `.dmg` 可双击启动；README 完整；LICENSE 合规。

| # | 开发单元 | 工期 | 前置 | 状态 |
|---|----------|------|------|------|
| 4.1 | PyInstaller 打包后端为 `backend.exe`（`--onedir`） | 1d | M7 | ⬜ |
| 4.2 | `electron-builder` 配置（`extraResources` 嵌入 backend） | 1d | 4.1, M12 | ⬜ |
| 4.3 | Windows `.exe` / macOS `.dmg` 构建验证 | 1d | 4.2 | ⬜ |
| 4.4 | `README.md` + `LICENSE`（MIT/Apache-2.0）+ 安装指南 | 1d | 4.3 | ⬜ |

---

## 六、关键决策点与风险应对

| 决策点 | 触发条件 | 建议决策 |
|--------|----------|----------|
| keyring 跨平台可行性 | M6 启动前（1.23 设置 API） | 先在 Windows + macOS 双平台验证 `keyring` 可用性，如不可行降级为 Fernet 固定密钥 + 系统密钥链手动方案 |
| SSE 断线重连策略 | M5 测试阶段 | 确认 `useSSE.ts` 重连 3 次（1s/2s/4s）+ `last_seq` 断点续传，避免重复输出 |
| 扫描版 PDF 处理 | M3 测试阶段 | 单页 < 50 字符标记 `is_scanned=true`，跳过 AI 提取，仅保留原文供手动阅读 |
| 打包体积控制 | Step 4 启动前 | 预期 150-200MB，如超标考虑 `pymupdf` 精简或后端按需加载 |
| 模型迭代适配 | 长期 | 模型名配置化（`config.py` 一行切换），不硬编码模型版本 |

| 风险 | 影响 | 应对 |
|------|------|------|
| Kimi API 限流（50 篇批量提取） | 高 | 串行 + 1 秒间隔 + 指数退避重试 |
| 大 PDF 解析超时（>50MB 扫描版） | 中 | 文件上限 50MB；扫描版标记后跳过 AI 提取 |
| Electron 打包体积大 | 低 | 预期 150-200MB，可接受 |
| Windows Defender 误杀 | 中 | README 说明首次启动可能触发 SmartScreen |
| 孤儿进程残留 | 高 | SIGTERM + SIGKILL 双保险 + 关闭测试验证 |

---

## 七、开发纪律检查单

每个 Milestone 完成后，必须逐项确认：

- [ ] **先查后改**：本次变更是否基于实际代码库状态，而非推断
- [ ] **独立模块**：新文件是否放在独立路由/独立文件，未污染已有模块
- [ ] **字段精确**：DB 字段 / API Schema 是否与 SPEC 文档完全一致
- [ ] **路由顺序**：固定路径路由是否在动态路径之前注册
- [ ] **代码清理**：旧逻辑是否直接删除（未标记 DEPRECATED）
- [ ] **Git 提交**：是否全部验证通过后再统一提交
- [ ] **敏感信息**：API Key 是否通过 keyring 加密，未硬编码
- [ ] **SSE 安全**：客户端断开时是否正确取消 asyncio 协程
- [ ] **测试覆盖**：新增功能是否伴随 pytest / 端到端测试
- [ ] **文档同步**：API 变更是否同步更新 OpenAPI 文档（自动）

---

## 八、版本历史

| 版本 | 日期 | 变更说明 |
|------|------|----------|
| v1.0 | 2026-06-04 | 初始版本，基于 SPEC v1.0 制定 |
