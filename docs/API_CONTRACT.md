# API 契约文档

> 状态：框架版（待开发过程中填充）  
> 作用：前后端开发合同，后端验证通过后锁定，前端据此动工  
> 基于：SPEC 文档第 6 章 API 接口

---

## 变更记录

| 日期 | 版本 | 变更说明 | 锁定状态 |
|------|------|----------|----------|
| 2026-06-04 | v0.1 | 框架初始化，仅列路径和状态 | 🔓 未锁定 |

---

## 一、健康检查

| 方法 | 路径 | 状态 | 锁定日期 | 验证方式 |
|------|------|------|----------|----------|
| GET | `/health` | ⬜ 待开发 | — | — |

**响应 Schema**（待填充）：
```json
{
  "status": "ok",
  "version": "string"
}
```

---

## 二、批次管理（`/api/v1/batches`）

| 方法 | 路径 | 状态 | 锁定日期 | 验证方式 |
|------|------|------|----------|----------|
| POST | `/` | ⬜ 待开发 | — | — |
| GET | `/` | ⬜ 待开发 | — | — |
| GET | `/{id}` | ⬜ 待开发 | — | — |
| PUT | `/{id}` | ⬜ 待开发 | — | — |
| DELETE | `/{id}` | ⬜ 待开发 | — | — |

**请求/响应 Schema**（待开发后填充）

---

## 三、文献 CRUD（`/api/v1/literature`）

| 方法 | 路径 | 状态 | 锁定日期 | 验证方式 |
|------|------|------|----------|----------|
| POST | `/upload` | ⬜ 待开发 | — | — |
| POST | `/{id}/parse` | ⬜ 待开发 | — | — |
| GET | `/` | ⬜ 待开发 | — | — |
| GET | `/{id}` | ⬜ 待开发 | — | — |
| GET | `/{id}/pages` | ⬜ 待开发 | — | — |
| GET | `/{id}/page/{page_num}` | ⬜ 待开发 | — | — |
| PUT | `/{id}/tags` | ⬜ 待开发 | — | — |
| DELETE | `/{id}` | ⬜ 待开发 | — | — |
| GET | `/tags` | ⬜ 待开发 | — | — |
| GET | `/stats` | ⬜ 待开发 | — | — |
| POST | `/{id}/extract` | ⬜ 待开发 | — | — |
| POST | `/{id}/translate` | ⬜ 待开发 | — | — |

**请求/响应 Schema**（待开发后填充）

---

## 四、文献问答（`/api/v1/literature/chat`）

| 方法 | 路径 | 状态 | 锁定日期 | 验证方式 |
|------|------|------|----------|----------|
| POST | `/chat` | ⬜ 待开发 | — | — |
| POST | `/{id}/chat` | ⬜ 待开发 | — | — |

**SSE 流格式**（待开发后填充）：
```
data: {"type": "chunk", "content": "..."}



data: {"type": "done"}



data: {"type": "error", "message": "..."}



```

---

## 五、批量提取（`/api/v1/batches/batch-extract`）

| 方法 | 路径 | 状态 | 锁定日期 | 验证方式 |
|------|------|------|----------|----------|
| POST | `/batches/batch-extract` | ⬜ 待开发 | — | — |

**SSE 进度事件格式**（待开发后填充）：
```
data: {"type": "progress", "current": 3, "total": 50, "paper_id": "...", "status": "extracting", "title": "..."}



data: {"type": "done", "success_count": 48, "fail_count": 2}



```

---

## 六、文献导出（`/api/v1/literature/export`）

| 方法 | 路径 | 状态 | 锁定日期 | 验证方式 |
|------|------|------|----------|----------|
| GET | `/export` | ⬜ 待开发 | — | — |

---

## 七、会话历史（`/api/v1/chat-sessions`）

| 方法 | 路径 | 状态 | 锁定日期 | 验证方式 |
|------|------|------|----------|----------|
| GET | `/chat-sessions` | ⬜ 待开发 | — | — |
| GET | `/{id}` | ⬜ 待开发 | — | — |
| DELETE | `/{id}` | ⬜ 待开发 | — | — |

---

## 八、论文撰写（`/api/v1/paper`）

| 方法 | 路径 | 状态 | 锁定日期 | 验证方式 |
|------|------|------|----------|----------|
| GET | `/blocks` | ⬜ 待开发 | — | — |
| PUT | `/blocks/{block_name}` | ⬜ 待开发 | — | — |
| POST | `/polish` | ⬜ 待开发 | — | — |
| POST | `/citation` | ⬜ 待开发 | — | — |

---

## 九、设置（`/api/v1/settings`）

| 方法 | 路径 | 状态 | 锁定日期 | 验证方式 |
|------|------|------|----------|----------|
| GET | `/` | ⬜ 待开发 | — | — |
| PUT | `/` | ⬜ 待开发 | — | — |
| POST | `/test` | ⬜ 待开发 | — | — |

**API Key 存储方案**（参考 SECURITY.md）：
- 加密密钥：系统 keychain（`keyring`）
- 密文：Fernet 加密后存 SQLite `settings` 表
- 首次设置：前端 HTTP → 后端从 keychain 读取/生成密钥 → 加密 → 存 SQLite
- 后续启动：后端从 keychain 读取密钥 → 解密 → 使用

---

## 十、数据管理（`/api/v1/data`）

> ⚠️ **仅限 localhost 访问**，拒绝远程请求。

| 方法 | 路径 | 状态 | 锁定日期 | 验证方式 |
|------|------|------|----------|----------|
| GET | `/db-stats` | ⬜ 待开发 | — | — |
| POST | `/export-db` | ⬜ 待开发 | — | — |
| POST | `/reset-db` | ⬜ 待开发 | — | — |

---

## 锁定规则

1. **后端开发完成** → Postman 验证通过 → 填写 Schema → 标记 🔒 锁定
2. **锁定后** → 前端才能依据此文档动工
3. **如需变更** → 先修改后端 → 重新验证 → 更新本文档 → 通知前端适配
4. **禁止前端在锁定前依据推断开发**
