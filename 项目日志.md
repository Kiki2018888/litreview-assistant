# ResearchAssistant 项目日志

> 记录每日开发步骤、问题、决策与验收结果  
> 使用方式：每日新建一节，按模板填写；关键问题单独标记 `[BLOCKER]`

---

## 日志模板（复制此节作为每日起点）

### YYYY-MM-DD（周X）

#### 今日目标
- [ ] 目标 1：...
- [ ] 目标 2：...

#### 执行记录

| 时间 | 任务 | 内容摘要 | 产出 |
|------|------|----------|------|
| 09:00 | 1.1 目录结构 | 初始化 backend/ 目录，创建 requirements.txt | 文件已提交 |
| 14:00 | 1.2 config.py | 配置中心：API Key / 模型 / 路径 / 超时 | 待验收 |

#### 关键问题与决策

| 问题 | 决策 | 影响范围 | 后续跟进 |
|------|------|----------|----------|
| ... | ... | ... | ... |

#### 代码变更清单

```
新增：
  - backend/config.py
  - backend/requirements.txt
修改：
  - （无）
删除：
  - （无）
```

#### 验收结果
- [ ] 单元测试通过
- [ ] Postman / 浏览器验证通过
- [ ] 代码审查检查单完成
- [ ] Git 提交（commit hash: `xxx`）

#### 明日计划
1. ...
2. ...

---

## 实际日志（从下方开始填写）

---

### 2026-06-04（周四）

#### 今日目标
- [ ] 完成项目进度规划与日志模板制定
- [ ] 确定 Step 1 首个开发单元（M1 脚手架）

#### 执行记录

| 时间 | 任务 | 内容摘要 | 产出 |
|------|------|----------|------|
| 18:12 | 进度规划 | 基于 SPEC 文档制定 4 Step / 12 Milestone / 33 单元规划 | 本文档 |
| 18:16 | 日志模板 | 设计每日开发日志模板，含时间/问题/决策/验收字段 | 本文档 |

#### 关键问题与决策

| 问题 | 决策 | 影响范围 | 后续跟进 |
|------|------|----------|----------|
| 开发指令如何防止上下文漂移 | 采用结构化模板（Step 1-9），单任务文件上限 5 个，接口契约冻结机制 | 全部开发单元 | 每条指令必须附"现状排查" |
| 前后端如何协调 | 严格执行"后端契约冻结后前端才能动工"，API 验证通过后 Schema 锁定 | Step 2 启动点 | M7 完成后确认接口冻结 |

#### 代码变更清单

```
新增：
  - /docs/项目进度规划.md
  - /docs/项目日志.md
修改：
  - （无）
删除：
  - （无）
```

#### 验收结果
- [x] 文档完整性检查
- [x] 里程碑依赖关系无循环
- [x] 开发单元粒度适中（0.5-1.5d）

#### 明日计划
1. 启动 M1：项目脚手架（1.1 目录结构 + 1.2 config.py + 1.3 main.py）
2. 准备 1.4 SQLAlchemy 模型定义的指令（先查后改模板）

---

### 2026-06-05（周五）

#### 今日目标
- [x] M4 文献 CRUD API 实现（10 个端点）

#### 执行记录

| 时间 | 任务 | 内容摘要 | 产出 |
|------|------|----------|------|
| 全天 | M4 文献 CRUD | 实现 10 个 REST API 端点，含批量上传、列表分页筛选、详情、页面原文、标签管理、删除、统计 | `literature_crud.py`（755 行） |
| 全天 | Schema 补充 | 新增 PaperListItem、PaperDetailResponse、PaperUploadResult/Response、PaperParseResponse、TagSummaryItem/Response、PaperStatsResponse、DeleteResponse、PageTextResponse | `schemas.py` |
| 全天 | 路由注册 | 创建 main.py，配置 CORS + health check，按正确顺序注册路由（固定路径 → 参数化路径） | `main.py` |

#### 关键问题与决策

| 问题 | 决策 | 影响范围 | 后续跟进 |
|------|------|----------|----------|
| FTS5 全文搜索无法纯 ORM 实现 | 使用 `text()` 查询 `papers_fts` 虚拟表获取 rowid，再通过 `literal_column("rowid").in_(ids)` 过滤 ORM 查询 | 文献列表搜索 | — |
| 标签 N+1 查询问题 | 实现 `_batch_load_tags()` 一次查询所有页面对应的标签 | 文献列表性能 | — |
| PDF 解析是同步 IO | 使用 `asyncio.to_thread()` 将 pdfplumber/pymupdf 调用放入线程池 | 上传/解析接口 | — |
| 删除文献需同时更新批次计数 | 在事务内完成：删文件 → 更新 batch paper_count → 删 DB 记录 | 批次统计准确性 | — |

#### 代码变更清单

```
新增：
  - backend/api/v1/literature_crud.py（755 行，10 个 API 端点）
  - backend/main.py（FastAPI 应用入口，CORS + health + 路由注册）
修改：
  - backend/models/schemas.py（新增 8 个响应/请求 Schema）
删除：
  - （无）
```

#### 验收结果
- [x] `GET /health` 返回 `{"status":"ok","version":"1.0.0"}`
- [x] `GET /docs` Swagger 正常显示所有路由
- [x] 路由顺序验证通过：固定路径（upload/tags/stats/list）→ 参数化路径（{paper_id}/*）
- [x] 零 lint 错误（3 个文件）
- [x] 模块导入测试全部通过
- [x] Git 提交（commit hash: `f860358`）

#### 明日计划
1. 周一：Cursor 审阅 M4 代码（`literature_crud.py` + `schemas.py` + `main.py`）
2. 审阅通过后修复问题
3. 进入 M5 核心 AI 链路（单篇提取 / 批量提取 / 跨文献问答 / 单篇精读）

---

### 2026-06-06（周六）

#### 今日目标
- [x] M5 核心 AI 链路：单篇提取 / 批量提取 / 跨文献问答 / 单篇精读
- [x] M4 审阅修复：FTS 转义 / CORS / 重解析清理 / 上传字段 / 统计字段
- [x] M5 审阅修复：空批次崩溃 / retry 绕过 / SSE 断开检测 / 并发锁 / 事务合并

#### 执行记录

| 时间 | 任务 | 内容摘要 | 产出 |
|------|------|----------|------|
| 上午 | M4 审阅修复 | 修复 5 个 P0+P1 问题：FTS5 转义函数、CORS regex、reparse 清理 ExtractedData、upload 响应字段名、stats 含 extracting | `literature_crud.py`、`main.py`、`schemas.py` |
| 下午 | M5 单篇提取 | `POST /{id}/extract` SSE 流，JSON Mode 结构化提取，asyncio.Lock 防并发 | `literature_extract.py`（280+ 行） |
| 下午 | M5 批量提取 | `POST /batches/batch-extract` SSE 进度流，batch_id 可选（全局 pending） | `batch_extract.py`（180+ 行） |
| 下午 | M5 跨文献/单篇问答 | `POST /chat`（跨文献）+ `POST /{id}/chat`（单篇），use_fulltext 开关，会话历史 | `literature_chat.py`（350+ 行） |
| 晚间 | M5 审阅修复 | 6 个 P0+P1：空批次越界、KimiClient.chat_stream 替代直接调用、SSE request.is_disconnected 检测、batch_id optional、asyncio.Lock 并发锁、单事务写入 | 上述 3 个文件 |

#### 关键问题与决策

| 问题 | 决策 | 影响范围 | 后续跟进 |
|------|------|----------|----------|
| sse_starlette 不在依赖列表 | 改用 FastAPI 原生 `StreamingResponse(media_type="text/event-stream")` | M5 全部 SSE 端点 | — |
| kimi_client.py 不支持 response_format | 抽取层内联 retry 循环（MAX_RETRIES + 指数退避），复用重试参数 | 单篇/批量提取 | — |
| 并发提取重复扣费 | per-paper `asyncio.Lock` + status 状态机：extracting→409, extract_failed→400 | 提取 API | — |
| SSE 断开检测 | `await request.is_disconnected()` + try/finally 回滚 extracting 状态 | 全部 SSE 端点 | — |

#### 代码变更清单

```
新增：
  - backend/api/v1/literature_extract.py（单篇 SSE 提取）
  - backend/api/v1/batch_extract.py（批量 SSE 提取）
  - backend/api/v1/literature_chat.py（跨文献 + 单篇对话）
修改：
  - backend/main.py（注册 M5 路由）
  - backend/api/v1/literature_crud.py（M4 审阅修复）
  - backend/models/schemas.py（M4 审阅修复）
删除：
  - （无）
```

#### 验收结果
- [x] 单篇提取 SSE 流正常，JSON Mode 输出完整
- [x] 批量提取进度事件正常，间隔 1s
- [x] 跨文献/单篇问答 SSE 流正常，会话历史写入 chat_sessions
- [x] 路由注册无冲突（`/chat` 在 `/{paper_id}/chat` 之前）
- [x] M5 审阅 P0 问题全部修复：空批次不崩溃、retry 对齐、断开检测、并发锁
- [x] 零 lint 错误
- [x] Git 提交（commit hash: `fdd82bb`、`5667bca`、`71a7902`）

#### 明日计划
1. M6 辅助模块：翻译 / 批次管理 / 设置（keyring 加密）/ 数据管理 / 会话历史
2. M6 审阅修复与闭环

---

### 2026-06-07（周日）

#### 今日目标
- [x] M6 辅助模块：翻译、批次管理、设置、数据管理、会话历史（5 模块 / 11 路由）
- [x] M6 审阅修复：kimi_client Fernet 解密接入 + reset-db FTS5 重建

#### 执行记录

| 时间 | 任务 | 内容摘要 | 产出 |
|------|------|----------|------|
| 上午 | M6 翻译 | `POST /{id}/translate` 摘要翻译，缓存复用，中文学术格式 | `literature_translate.py` |
| 上午 | M6 批次管理 | CRUD：列表/详情/更新/删除，删除前 papers SET NULL | `batches.py` |
| 上午 | M6 设置 | Fernet + keyring 三层加密，API Key 脱敏，测试连接 | `settings.py`（350+ 行） |
| 下午 | M6 数据管理 | localhost-only：db-stats、export-db、reset-db | `data.py` |
| 下午 | M6 会话历史 | 列表分页/详情/删除，session_type 筛选 | `chat_sessions.py` |
| 晚间 | M6 审阅修复 | P0-1: kimi_client._get_api_key 接入 settings._get_decrypted_api_key；P0-2: reset-db 后 _init_fts5() 重建虚拟表+6 触发器 | `kimi_client.py`、`data.py` |

#### 关键问题与决策

| 问题 | 决策 | 影响范围 | 后续跟进 |
|------|------|----------|----------|
| Fernet 密钥跨平台存储 | keyring → 降级文件 → 新建 三层策略，Windows/macOS 均可用 | 设置 API、kimi_client | M7 验收时双平台验证 |
| API Key 密文与 kimi_client 的集成断裂 | `_get_api_key()` 改为委托 `settings._get_decrypted_api_key()`，不重复实现解密 | 全部 AI 调用链路 | — |
| reset-db 后 FTS5 缺失 | 提取 Alembic 迁移中的 FTS5 SQL（1 虚拟表 + 6 触发器），封装 `_init_fts5()` | 数据管理 API、全文搜索 | — |
| 批次路由与批量提取路由前缀冲突 | 两者共用 `/api/v1/batches`，但路径不同（`/{id}` vs `/batch-extract`），无冲突 | 路由注册 | — |

#### 代码变更清单

```
新增：
  - backend/api/v1/literature_translate.py（翻译 API）
  - backend/api/v1/batches.py（批次 CRUD）
  - backend/api/v1/settings.py（设置 + Fernet 加密）
  - backend/api/v1/data.py（数据管理 localhost-only）
  - backend/api/v1/chat_sessions.py（会话历史）
修改：
  - backend/main.py（注册 5 个新路由，总计 33 路由）
  - backend/services/kimi_client.py（_get_api_key 接入 Fernet 解密）
  - backend/api/v1/data.py（reset-db 后 _init_fts5）
删除：
  - （无）
```

#### 验收结果
- [x] 所有 33 个路由无冲突，Swagger 正常显示
- [x] 设置页保存 API Key 后，kimi_client 能正确解密调用 Kimi API
- [x] reset-db 后 FTS5 虚拟表 + 6 触发器完整重建
- [x] Fernet 加密方案：keyring 优先，本地降级文件兜底
- [x] 零 lint 错误，Python 编译通过
- [x] Git 提交（commit hash: `9946ccb`、`925e23e`）

#### 明日计划
1. M7 后端验收：pytest 单元测试覆盖核心服务 + 33 个 API 端点
2. Postman 集成测试：上传→解析→提取→问答 完整链路验证

---

## 附录：快速索引

### 里程碑状态速查

| Milestone | 名称 | 计划日期 | 实际完成 | 状态 |
|-----------|------|----------|----------|------|
| M1 | 项目脚手架 | Week 1 Day 1-2 | — | ✅ |
| M2 | 数据层 | Week 1 Day 2-4 | — | ✅ |
| M3 | 基础服务 | Week 1 Day 4-6 | — | ✅ |
| M4 | 文献 CRUD | Week 1 Day 6-9 | 2026-06-05 | ✅ |
| M5 | 核心 AI 链路 | Week 2 Day 9-14 | 2026-06-06 | ✅ |
| M6 | 辅助模块 | Week 2 Day 14-16 | 2026-06-07 | ✅ |
| M7 | 后端验收 | Week 2 Day 16-18 | — | ⬜ |
| M8 | Electron 壳 | Week 3 Day 18-20 | — | ⬜ |
| M9 | React 骨架 | Week 3 Day 20-22 | — | ⬜ |
| M10 | 核心组件 | Week 3-4 Day 22-28 | — | ⬜ |
| M11 | 功能页面 | Week 4 Day 28-31 | — | ⬜ |
| M12 | 前端验收 | Week 4 Day 31-32 | — | ⬜ |
| Step 3 | 论文撰写 | Week 5 | — | ⬜ |
| Step 4 | 打包发布 | Week 6 | — | ⬜ |

### 阻塞问题跟踪

| 日期 | 问题描述 | 状态 | 解决日期 | 解决方案 |
|------|----------|------|----------|----------|
| — | — | — | — | — |

### 接口契约冻结记录

| API | 冻结日期 | 验证方式 | 变更历史 |
|-----|----------|----------|----------|
| POST /upload | — | Postman | — |
| POST /{id}/extract | — | Postman + SSE | — |
| POST /chat | — | Postman + SSE | — |
| POST /{id}/chat | — | Postman + SSE | — |
| GET /settings | — | Postman | — |
| PUT /settings | — | Postman | — |
