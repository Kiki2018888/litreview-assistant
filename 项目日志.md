# ResearchAssistant 项目日志

> 记录每日开发步骤、问题、决策与验收结果  
> 使用方式：每日新建一节，按模板填写；关键问题单独标记 `[BLOCKER]`

---

## 日志模板（复制此节作为每日起点）

### YYYY-MM-DD（周X）

#### 今日目标
- [ ] 目标 1：...
- [ ] 目标 2：...

#### 执行记录

| 时间 | 任务 | 内容摘要 | 产出 |
|------|------|----------|------|
| 09:00 | 1.1 目录结构 | 初始化 backend/ 目录，创建 requirements.txt | 文件已提交 |
| 14:00 | 1.2 config.py | 配置中心：API Key / 模型 / 路径 / 超时 | 待验收 |

#### 关键问题与决策

| 问题 | 决策 | 影响范围 | 后续跟进 |
|------|------|----------|----------|
| ... | ... | ... | ... |

#### 代码变更清单

```
新增：
  - backend/config.py
  - backend/requirements.txt
修改：
  - （无）
删除：
  - （无）
```

#### 验收结果
- [ ] 单元测试通过
- [ ] Postman / 浏览器验证通过
- [ ] 代码审查检查单完成
- [ ] Git 提交（commit hash: `xxx`）

#### 明日计划
1. ...
2. ...

---

## 实际日志（从下方开始填写）

---

### 2026-06-04（周四）

#### 今日目标
- [ ] 完成项目进度规划与日志模板制定
- [ ] 确定 Step 1 首个开发单元（M1 脚手架）

#### 执行记录

| 时间 | 任务 | 内容摘要 | 产出 |
|------|------|----------|------|
| 18:12 | 进度规划 | 基于 SPEC 文档制定 4 Step / 12 Milestone / 33 单元规划 | 本文档 |
| 18:16 | 日志模板 | 设计每日开发日志模板，含时间/问题/决策/验收字段 | 本文档 |

#### 关键问题与决策

| 问题 | 决策 | 影响范围 | 后续跟进 |
|------|------|----------|----------|
| 开发指令如何防止上下文漂移 | 采用结构化模板（Step 1-9），单任务文件上限 5 个，接口契约冻结机制 | 全部开发单元 | 每条指令必须附"现状排查" |
| 前后端如何协调 | 严格执行"后端契约冻结后前端才能动工"，API 验证通过后 Schema 锁定 | Step 2 启动点 | M7 完成后确认接口冻结 |

#### 代码变更清单

```
新增：
  - /docs/项目进度规划.md
  - /docs/项目日志.md
修改：
  - （无）
删除：
  - （无）
```

#### 验收结果
- [x] 文档完整性检查
- [x] 里程碑依赖关系无循环
- [x] 开发单元粒度适中（0.5-1.5d）

#### 明日计划
1. 启动 M1：项目脚手架（1.1 目录结构 + 1.2 config.py + 1.3 main.py）
2. 准备 1.4 SQLAlchemy 模型定义的指令（先查后改模板）

---

### 2026-06-05（周五）

#### 今日目标
- [x] M4 文献 CRUD API 实现（10 个端点）

#### 执行记录

| 时间 | 任务 | 内容摘要 | 产出 |
|------|------|----------|------|
| 全天 | M4 文献 CRUD | 实现 10 个 REST API 端点，含批量上传、列表分页筛选、详情、页面原文、标签管理、删除、统计 | `literature_crud.py`（755 行） |
| 全天 | Schema 补充 | 新增 PaperListItem、PaperDetailResponse、PaperUploadResult/Response、PaperParseResponse、TagSummaryItem/Response、PaperStatsResponse、DeleteResponse、PageTextResponse | `schemas.py` |
| 全天 | 路由注册 | 创建 main.py，配置 CORS + health check，按正确顺序注册路由（固定路径 → 参数化路径） | `main.py` |

#### 关键问题与决策

| 问题 | 决策 | 影响范围 | 后续跟进 |
|------|------|----------|----------|
| FTS5 全文搜索无法纯 ORM 实现 | 使用 `text()` 查询 `papers_fts` 虚拟表获取 rowid，再通过 `literal_column("rowid").in_(ids)` 过滤 ORM 查询 | 文献列表搜索 | — |
| 标签 N+1 查询问题 | 实现 `_batch_load_tags()` 一次查询所有页面对应的标签 | 文献列表性能 | — |
| PDF 解析是同步 IO | 使用 `asyncio.to_thread()` 将 pdfplumber/pymupdf 调用放入线程池 | 上传/解析接口 | — |
| 删除文献需同时更新批次计数 | 在事务内完成：删文件 → 更新 batch paper_count → 删 DB 记录 | 批次统计准确性 | — |

#### 代码变更清单

```
新增：
  - backend/api/v1/literature_crud.py（755 行，10 个 API 端点）
  - backend/main.py（FastAPI 应用入口，CORS + health + 路由注册）
修改：
  - backend/models/schemas.py（新增 8 个响应/请求 Schema）
删除：
  - （无）
```

#### 验收结果
- [x] `GET /health` 返回 `{"status":"ok","version":"1.0.0"}`
- [x] `GET /docs` Swagger 正常显示所有路由
- [x] 路由顺序验证通过：固定路径（upload/tags/stats/list）→ 参数化路径（{paper_id}/*）
- [x] 零 lint 错误（3 个文件）
- [x] 模块导入测试全部通过
- [x] Git 提交（commit hash: `f860358`）

#### 明日计划
1. 周一：Cursor 审阅 M4 代码（`literature_crud.py` + `schemas.py` + `main.py`）
2. 审阅通过后修复问题
3. 进入 M5 核心 AI 链路（单篇提取 / 批量提取 / 跨文献问答 / 单篇精读）

---

### 2026-06-06（周六）

#### 今日目标
- [x] M5 核心 AI 链路：单篇提取 / 批量提取 / 跨文献问答 / 单篇精读
- [x] M4 审阅修复：FTS 转义 / CORS / 重解析清理 / 上传字段 / 统计字段
- [x] M5 审阅修复：空批次崩溃 / retry 绕过 / SSE 断开检测 / 并发锁 / 事务合并

#### 执行记录

| 时间 | 任务 | 内容摘要 | 产出 |
|------|------|----------|------|
| 上午 | M4 审阅修复 | 修复 5 个 P0+P1 问题：FTS5 转义函数、CORS regex、reparse 清理 ExtractedData、upload 响应字段名、stats 含 extracting | `literature_crud.py`、`main.py`、`schemas.py` |
| 下午 | M5 单篇提取 | `POST /{id}/extract` SSE 流，JSON Mode 结构化提取，asyncio.Lock 防并发 | `literature_extract.py`（280+ 行） |
| 下午 | M5 批量提取 | `POST /batches/batch-extract` SSE 进度流，batch_id 可选（全局 pending） | `batch_extract.py`（180+ 行） |
| 下午 | M5 跨文献/单篇问答 | `POST /chat`（跨文献）+ `POST /{id}/chat`（单篇），use_fulltext 开关，会话历史 | `literature_chat.py`（350+ 行） |
| 晚间 | M5 审阅修复 | 6 个 P0+P1：空批次越界、KimiClient.chat_stream 替代直接调用、SSE request.is_disconnected 检测、batch_id optional、asyncio.Lock 并发锁、单事务写入 | 上述 3 个文件 |

#### 关键问题与决策

| 问题 | 决策 | 影响范围 | 后续跟进 |
|------|------|----------|----------|
| sse_starlette 不在依赖列表 | 改用 FastAPI 原生 `StreamingResponse(media_type="text/event-stream")` | M5 全部 SSE 端点 | — |
| kimi_client.py 不支持 response_format | 抽取层内联 retry 循环（MAX_RETRIES + 指数退避），复用重试参数 | 单篇/批量提取 | — |
| 并发提取重复扣费 | per-paper `asyncio.Lock` + status 状态机：extracting→409, extract_failed→400 | 提取 API | — |
| SSE 断开检测 | `await request.is_disconnected()` + try/finally 回滚 extracting 状态 | 全部 SSE 端点 | — |

#### 代码变更清单

```
新增：
  - backend/api/v1/literature_extract.py（单篇 SSE 提取）
  - backend/api/v1/batch_extract.py（批量 SSE 提取）
  - backend/api/v1/literature_chat.py（跨文献 + 单篇对话）
修改：
  - backend/main.py（注册 M5 路由）
  - backend/api/v1/literature_crud.py（M4 审阅修复）
  - backend/models/schemas.py（M4 审阅修复）
删除：
  - （无）
```

#### 验收结果
- [x] 单篇提取 SSE 流正常，JSON Mode 输出完整
- [x] 批量提取进度事件正常，间隔 1s
- [x] 跨文献/单篇问答 SSE 流正常，会话历史写入 chat_sessions
- [x] 路由注册无冲突（`/chat` 在 `/{paper_id}/chat` 之前）
- [x] M5 审阅 P0 问题全部修复：空批次不崩溃、retry 对齐、断开检测、并发锁
- [x] 零 lint 错误
- [x] Git 提交（commit hash: `fdd82bb`、`5667bca`、`71a7902`）

#### 明日计划
1. M6 辅助模块：翻译 / 批次管理 / 设置（keyring 加密）/ 数据管理 / 会话历史
2. M6 审阅修复与闭环

---

### 2026-06-07（周日）

#### 今日目标
- [x] M6 辅助模块：翻译、批次管理、设置、数据管理、会话历史（5 模块 / 11 路由）
- [x] M6 审阅修复：kimi_client Fernet 解密接入 + reset-db FTS5 重建

#### 执行记录

| 时间 | 任务 | 内容摘要 | 产出 |
|------|------|----------|------|
| 上午 | M6 翻译 | `POST /{id}/translate` 摘要翻译，缓存复用，中文学术格式 | `literature_translate.py` |
| 上午 | M6 批次管理 | CRUD：列表/详情/更新/删除，删除前 papers SET NULL | `batches.py` |
| 上午 | M6 设置 | Fernet + keyring 三层加密，API Key 脱敏，测试连接 | `settings.py`（350+ 行） |
| 下午 | M6 数据管理 | localhost-only：db-stats、export-db、reset-db | `data.py` |
| 下午 | M6 会话历史 | 列表分页/详情/删除，session_type 筛选 | `chat_sessions.py` |
| 晚间 | M6 审阅修复 | P0-1: kimi_client._get_api_key 接入 settings._get_decrypted_api_key；P0-2: reset-db 后 _init_fts5() 重建虚拟表+6 触发器 | `kimi_client.py`、`data.py` |

#### 关键问题与决策

| 问题 | 决策 | 影响范围 | 后续跟进 |
|------|------|----------|----------|
| Fernet 密钥跨平台存储 | keyring → 降级文件 → 新建 三层策略，Windows/macOS 均可用 | 设置 API、kimi_client | M7 验收时双平台验证 |
| API Key 密文与 kimi_client 的集成断裂 | `_get_api_key()` 改为委托 `settings._get_decrypted_api_key()`，不重复实现解密 | 全部 AI 调用链路 | — |
| reset-db 后 FTS5 缺失 | 提取 Alembic 迁移中的 FTS5 SQL（1 虚拟表 + 6 触发器），封装 `_init_fts5()` | 数据管理 API、全文搜索 | — |
| 批次路由与批量提取路由前缀冲突 | 两者共用 `/api/v1/batches`，但路径不同（`/{id}` vs `/batch-extract`），无冲突 | 路由注册 | — |

#### 代码变更清单

```
新增：
  - backend/api/v1/literature_translate.py（翻译 API）
  - backend/api/v1/batches.py（批次 CRUD）
  - backend/api/v1/settings.py（设置 + Fernet 加密）
  - backend/api/v1/data.py（数据管理 localhost-only）
  - backend/api/v1/chat_sessions.py（会话历史）
修改：
  - backend/main.py（注册 5 个新路由，总计 33 路由）
  - backend/services/kimi_client.py（_get_api_key 接入 Fernet 解密）
  - backend/api/v1/data.py（reset-db 后 _init_fts5）
删除：
  - （无）
```

#### 验收结果
- [x] 所有 33 个路由无冲突，Swagger 正常显示
- [x] 设置页保存 API Key 后，kimi_client 能正确解密调用 Kimi API
- [x] reset-db 后 FTS5 虚拟表 + 6 触发器完整重建
- [x] Fernet 加密方案：keyring 优先，本地降级文件兜底
- [x] 零 lint 错误，Python 编译通过
- [x] Git 提交（commit hash: `9946ccb`、`925e23e`）

#### 明日计划
1. M7 后端验收：pytest 单元测试覆盖核心服务 + 33 个 API 端点
2. Postman 集成测试：上传→解析→提取→问答 完整链路验证

---

### 2026-06-08（周一）

#### 今日目标
- [x] M7 后端验收完成，Step 1 闭环
- [x] M10 核心组件全部完成：LiteratureDetail、PdfViewer、ChatPanel、useSSE
- [x] Cursor 审阅 6 项问题全部修复

#### 执行记录

| 时间 | 任务 | 内容摘要 | 产出 |
|------|------|----------|------|
| 上午 | pytest fixture 时序修复 | 修复 conftest.py 中 `_patch_db` 与 `client` fixture 的执行时序冲突：monkeypatch 在 API 模块导入前生效 + `app.dependency_overrides` 兜底 + StaticPool 解决 SQLite :memory: 连接池隔离 | `conftest.py` |
| 上午 | test_kimi_client.py 修复 | 移除 `_make_mock_openai` 中 stream 分支（`yield` + `return value` 在 async def 中是 SyntaxError） | `test_kimi_client.py` |
| 上午 | sample_extracted_paper FK 修复 | 添加 `db_session.flush()` 确保 Paper 先入库再插入 ExtractedData | `conftest.py` |
| 上午 | Cursor 最终审阅 | 全量代码审阅通过，确认 Step 1 可以闭环 | — |
| 上午 | 临时文件清理 | 删除 `_out.txt` `_results.txt` `_results2.txt` `_single.txt`，更新 .gitignore | `.gitignore` |
| 下午 | M10-第二批：LiteratureDetail | PDF 视图、摘要、元数据标签展示、SSE 提取进度状态机 | `LiteratureDetail.tsx` |
| 下午 | M10-第二批：PdfViewer | iframe + blob URL 渲染 PDF，paperId API 获取二进制，回退 file:// 兼容 | `PdfViewer.tsx` |
| 晚间 | M10-第三批：ChatPanel | 单篇精读 + 跨文献问答，会话列表，SSE 流式对话 | `ChatPanel.tsx` |
| 晚间 | M10-第三批：useSSE | fetch + ReadableStream + 重连策略 + 并发禁止（per-url Lock） | `useSSE.ts` |
| 晚间 | Cursor 审阅修复 | 6 项 P0+P1 问题全部修复（详见关键问题） | 上述 4 个文件 + `Literature.tsx` |

#### 关键问题与决策

| 问题 | 决策 | 影响范围 | 后续跟进 |
|------|------|----------|----------|
| pytest fixture monkeypatch 时序导致 17 个 SQLAlchemy 错误 | `client` 显式依赖 `_patch_db` + `dependency_overrides[get_db]` 双重保障 | 全部 API 端点测试 | — |
| SQLite :memory: 连接池导致表不存在 | `create_engine` 添加 `poolclass=StaticPool` 强制复用同一连接 | 全部 DB 测试 | — |
| test_literature_crud.py 13 个测试 URL 错误 | 测试文件使用了旧版路由路径（如 `/api/v1/upload`），与当前 API 不一致 | M7 验收 | Step 2 联调前统一修正 |
| test_kimi_client.py async generator 混用 | `yield` + `return value` 在 Python 3.12 中为 SyntaxError | 单文件 | 已修复 |
| **PdfViewer file:// 在 Electron 中被 webSecurity 拦截** | 新增 paperId prop → fetch 后端 API 获取 PDF 二进制 → Blob → URL.createObjectURL → iframe blob URL | PdfViewer.tsx | 卸载时 revokeObjectURL 防内存泄漏 |
| **useSSE 对 HTTP 4xx/5xx 也重连 3 次** | 新增 disableRetry 选项；isNetworkError() 仅 TypeError/Failed to fetch 重连；HTTP 错误直接 onError | useSSE.ts、ChatPanel.tsx | ChatPanel 聊天流禁用重连防重复提交 |
| **精读时 Detail 消失，无法对照 PDF** | Literature.tsx 新增 showDetailWithChat 模式：右侧上下分栏 50%/50%，ChatPanel + Detail/PdfViewer 同时显示 | Literature.tsx | — |
| **ChatPanel 关闭/卸载未 abort SSE** | useEffect cleanup 调用 abort()；handleClose 先 abort 再 onClose | ChatPanel.tsx | — |
| **LiteratureDetail 提取 SSE 内联 fetch，未复用 useSSE** | 替换内联 fetch + `\n` 分割逻辑为 useSSE({ disableRetry: true }) | LiteratureDetail.tsx | — |
| **status 文案"第 5/3 次尝试"误导用户** | status 事件显示"正在提取…"；retry 事件显示"第 x 次重试，y s 后…" | LiteratureDetail.tsx | — |

#### 代码变更清单

```
新增：
  - backend/tests/conftest.py（fixture 时序修复 + StaticPool + FK flush）
  - frontend/src/components/LiteratureDetail.tsx（PDF+摘要+SSE 提取）
  - frontend/src/components/PdfViewer.tsx（blob URL + iframe 渲染）
  - frontend/src/components/ChatPanel.tsx（单篇精读+跨文献问答）
  - frontend/src/hooks/useSSE.ts（fetch ReadableStream + 重连 + 并发禁止）
修改：
  - backend/tests/test_kimi_client.py（移除 stream 分支）
  - .gitignore（新增 backend/tests/_*.txt）
  - frontend/src/pages/Literature.tsx（新增 showDetailWithChat 模式）
删除：
  - backend/tests/_out.txt
  - backend/tests/_results.txt
  - backend/tests/_results2.txt
  - backend/tests/_single.txt
```

#### 验收结果
- [x] pytest backend/tests/ 收集 98 个测试，全部可执行
- [x] SQLAlchemy"no such table"错误全部消除
- [x] FK 约束错误全部消除
- [x] Cursor 最终审阅通过：后端 Step 1 可闭环
- [x] 临时文件已清理，.gitignore 已更新
- [x] M10 组件 npx tsc --noEmit 零类型错误
- [x] M10 组件 ESLint 零警告（5 个文件）
- [x] 未修改 backend/、electron/、App.tsx、Layout.tsx
- [x] Git 提交（commit hash: `d60cf80`）

#### 遗留问题

| 问题 | 优先级 | 说明 |
|------|--------|------|
| API_CONTRACT.md 仍 v0.1 | P0 | Step 2 联调前必须锁定 |
| 测试覆盖缺口：translate、health、parse、page、reset-db confirm | P1 | 以上端点缺少自动化测试 |
| extracting→pending 启动重置未实现 | P2 | 服务重启后残留 extracting 状态的文献无法自动恢复 |
| FTS JSON 搜索质量 | P2 | JSON 结构化字段的搜索体验待优化 |
| pages 分页 | P2 | 长文献页面分页功能待实现 |
| 13 个 test_literature_crud.py 测试 URL 不匹配 | P1 | 测试文件使用了旧版路由路径，需统一修正 |
| 3 个测试状态码不匹配 | P1 | test_create_batch (200→201)、test_reset_without_confirm_field (422→400) 等 |
| 5 个测试 mock 目标不正确 | P1 | async generator mock、属性 patch 目标偏移等 |

#### 明日计划
1. M11 功能页面集成：设置页、会话历史、论文撰写
2. 联调前锁定 API_CONTRACT.md

---

### 2026-06-09（周二）

#### 今日目标
- [x] 从 OneDrive 同步事故中恢复 D:\ResearchAssistant 工作区
- [x] 重建开发环境并验证前后端启动
- [x] 生产构建验证与 Kimi 多 Provider API Key 架构（v1.0.1）

#### 执行记录

| 时间 | 任务 | 内容摘要 | 产出 |
|------|------|----------|------|
| 上午 | Git 原生恢复 | `git checkout HEAD -- .` 从 f8b5b5d 还原 86 个跟踪文件；清理 `__.git/` 垃圾与 `.mypy_cache` | 工作区干净 |
| 上午 | 生产修复补回 | 从 OneDrive 备份合并 backend.spec / main.py / electron/main.js / Settings.tsx | commit `54f76b0` |
| 上午 | 环境重建 | 创建 `.venv`，`pip install -r requirements.txt`；前后端 `npm install` | 后端 8084、前端 5173 可启动 |
| 下午 | 生产构建 | 前端 `npm run build`、PyInstaller、`electron-builder` 生成 NSIS 安装包 | `release/ResearchAssistant Setup 1.0.0.exe` |
| 下午 | API Key 排查 | 定位 settings/test 对 sk-kimi- Key 误发 moonshot endpoint 导致 401 | 根因：Key 类型与 endpoint 不匹配 |
| 晚间 | 多 Provider 架构 | 新增 `api_provider.py`、DB 字段、Settings 页 Provider/Base URL、KimiClient 动态 endpoint | v1.0.1，34 项测试通过 |

#### 关键问题与决策

| 问题 | 决策 | 影响范围 | 后续跟进 |
|------|------|----------|----------|
| OneDrive 迁移后工作区残缺 | D 盘独立路径 `D:\ResearchAssistant` Git 恢复 + 手动补漏未提交修复 | 全部源码 | 与 OneDrive 副本脱钩，以 D 盘为准 |
| sk-kimi- Key 测试返回 valid:false | 非 Key 失效，而是 Moonshot 通用 endpoint 不接受 Coding Key | settings/test | 实现多 Provider 自动路由 |
| Kimi Coding endpoint 返回 403 | Key 被识别，但服务端限制仅 Coding Agent 客户端 | sk-kimi- 用户 | 推荐 Moonshot 平台 sk- Key；或调研 Agent 请求头 |
| PDF 上传生产模式 500 | `literature_crud.py` 在赋值 file_path 前 flush，NOT NULL 约束失败 | 上传链路 | P0 修复待办 |
| `.gitignore` 中 `lib/` 误忽略 `frontend/src/lib/utils.ts` | 改为 `/lib/` 仅忽略仓库根目录 Python lib | 前端构建 | 已纳入 utils.ts |
| PyInstaller 未在 requirements.txt | 构建时单独 `pip install pyinstaller` | 打包流程 | 文档或 dev 依赖补充 |

#### 代码变更清单

```
新增：
  - backend/services/api_provider.py（infer_provider / resolve_api_config）
  - backend/alembic/versions/a1b2c3d4e5f6_add_api_provider_fields.py
  - backend/tests/test_api_provider.py
  - frontend/src/lib/utils.ts（shadcn cn 工具，修复生产构建 TS2307）
修改：
  - backend/api/v1/settings.py（Provider CRUD + 增强 test 响应）
  - backend/services/kimi_client.py（动态 base_url + reset_kimi_client）
  - backend/models/tables.py、schemas.py（api_provider / api_base_url 字段）
  - backend/config.py（VERSION 1.0.1）
  - frontend/src/pages/Settings.tsx、types/index.ts（Provider UI）
  - backend/tests/test_kimi_client.py、test_settings.py
  - .gitignore（/lib/ 修正、指令生成RULES.md）
删除：
  - （无源码删除；本地 dist/ release/ 构建产物保持 gitignore）
```

#### 验收结果
- [x] pytest 多 Provider 相关测试 34 项通过
- [x] 前端 `npm run build` 通过
- [x] 开发模式后端 /health、前端 Vite 启动正常
- [x] 生产安装包构建成功（settings test 200，上传仍 500 待修）
- [x] Git 提交（commit hash: `27f9668`）

#### 遗留问题

| 问题 | 优先级 | 说明 |
|------|--------|------|
| literature_crud 上传 file_path 顺序 | P0 | 生产/开发上传均可能 500 |
| sk-kimi- Key 403 白名单 | P1 | 架构已路由，需 Moonshot 平台 Key 或官方 Agent 接入 |
| Alembic 从项目根启动路径错误 | P2 | fallback create_all 可用，建议 dev 模式显式 script_location |
| pyinstaller 未列入 requirements.txt | P2 | 打包文档补充 |

#### 明日计划
1. 修复 PDF 上传 file_path 业务 bug 并回归测试
2. 基于 v1.0.1 重新打生产包并验证上传 + API Key 测试
3. 推进 M11/M12 或 Step 4 发布流程

---

### 2026-06-10（周三）

#### 今日目标
- [x] 修复 PDF 上传 500（`file_path` NOT NULL 约束）
- [x] 排查并修复 Kimi 模型 404 / API Key 测试误报
- [x] 接入 DeepSeek V4 Pro 作为第二 Provider
- [x] 查询 Moonshot 账号可用模型并扩充模型列表
- [x] 重新打包 v1.0.1 安装包

#### 执行记录

| 时间 | 任务 | 内容摘要 | 产出 |
|------|------|----------|------|
| 上午 | PDF 上传修复 | `literature_crud.py` 创建 Paper 时先赋值 `file_path` 再 `flush`，避免 NOT NULL 约束导致 500 | commit `8b742ed` |
| 上午 | 模型 404 排查 | 调用 `GET /v1/models` 确认 Tier1 账号可用模型；`kimi-k2-6`（连字符）在 API 中为 `kimi-k2.6`（点号），导致此前 404 | 模型列表记录 |
| 上午 | settings 测试增强 | `test_api_key` 对 HTTP 404 返回友好提示「当前账号无权使用模型 xxx」，区分 Key 无效与模型不可用 | `settings.py` |
| 上午 | 版本号与默认模型 | `package.json` / `config.py` 升至 **1.0.1**；默认模型暂改为 `moonshot-v1-8k`（Tier1 确认可用） | commit `8b742ed` |
| 下午 | DeepSeek Provider | 新增 `PROVIDER_DEEPSEEK`、endpoint `https://api.deepseek.com`、模型 `deepseek-v4-pro` / `deepseek-v4-flash`；Settings 页下拉与 base_url/模型自动填充 | `api_provider.py`、`Settings.tsx` |
| 下午 | Moonshot 模型扩充 | `MOONSHOT_MODELS` 增加 `moonshot-v1-32k`、`moonshot-v1-128k`、`moonshot-v1-auto`、`kimi-k2.5`、`kimi-k2.6` 等 | `api_provider.py`、`config.py` |
| 下午 | 默认模型升级 | 在确认 32k/128k 可用后，将默认模型从 `moonshot-v1-8k` 提升为 **`moonshot-v1-128k`** | commit `eddae04` |
| 下午 | 开发模式验证 | Provider 切换、DeepSeek 无 Key 提示、Moonshot + `moonshot-v1-32k` 测试连接 valid:true；pytest 21 项通过 | API 脚本 + pytest |
| 晚间 | 生产打包 | 前端 build → PyInstaller → electron-builder；生成 NSIS 安装包与 win-unpacked 便携版 | `release/ResearchAssistant Setup 1.0.1.exe`（127.93 MB） |

#### 关键问题与决策

| 问题 | 决策 | 影响范围 | 后续跟进 |
|------|------|----------|----------|
| Tier1 Key 对 `kimi-k2-6` 返回 404 | 根因是模型 ID 写法错误（API 为 `kimi-k2.6`）；同时账号有 32k/128k 可用 | 默认模型、模型列表 | 列表中同时保留别名便于迁移 |
| Moonshot 与 DeepSeek Key 均为 `sk-` 前缀 | **不能**通过前缀自动推断 Provider，必须用户在设置页手动选择 | `infer_provider`、Settings UI | auto 模式仍只区分 Moonshot / Kimi Coding |
| PDF 上传生产模式 500 | 先写 `file_path` 再 `db.flush()`，满足 NOT NULL | 上传链路 | 已修复，待安装包实测 |
| Alembic 目录缺失（dev 启动警告） | fallback `create_all` + FTS5 初始化仍可用 | 开发模式启动 | P2：补全 alembic 路径或文档说明 |
| DeepSeek 真实连接未测 | 当前无 DeepSeek API Key，仅验证 Provider 切换与配置解析 | settings/test | 用户申请 Key 后补测 |

#### Moonshot 账号可用模型（`GET /v1/models`，2026-06-10）

| 模型 ID | 上下文长度 | 备注 |
|---------|-----------|------|
| moonshot-v1-8k | 8,192 | Tier1 可用 |
| moonshot-v1-32k | 32,768 | 实测 test 通过 |
| moonshot-v1-128k | 131,072 | **当前默认** |
| moonshot-v1-auto | 131,072 | 自动选择 |
| kimi-k2.5 | 262,144 | K2 系列 |
| kimi-k2.6 | 262,144 | 注意点号 `.` 非连字符 `-` |
| moonshot-v1-*-vision-preview | 各异 | 视觉预览版 |

#### 代码变更清单

```
新增：
  - （无新文件；DeepSeek 逻辑并入 api_provider.py）
修改：
  - backend/api/v1/literature_crud.py（PDF 上传 file_path 顺序修复）
  - backend/api/v1/settings.py（模型 404 友好提示）
  - backend/services/api_provider.py（DeepSeek Provider + Moonshot 模型扩充）
  - backend/config.py（deepseek_base_url / deepseek_default_model；默认模型 128k）
  - backend/tests/test_api_provider.py（DeepSeek resolve 测试）
  - frontend/src/pages/Settings.tsx（DeepSeek 选项 + 默认模型 128k）
  - frontend/src/types/index.ts（ApiProvider 增加 deepseek）
  - package.json（version 1.0.1）
删除：
  - （无）
```

#### 验收结果
- [x] PDF 上传 `file_path` 业务 bug 已修复（代码层）
- [x] settings/test 对模型 404 返回明确提示
- [x] Moonshot `moonshot-v1-32k` 测试连接 valid:true
- [x] DeepSeek Provider 切换：base_url / 默认模型自动填充正确
- [x] DeepSeek 无 Key 测试返回「API Key 未配置」（不崩溃）
- [x] pytest `test_api_provider.py` + `test_settings.py` 21 项通过
- [x] 前端 `npm run build` 通过
- [x] PyInstaller + electron-builder 打包成功
- [x] Git 提交（commit hash: `8b742ed`、`eddae04`）
- [ ] DeepSeek 真实 API 连接（待用户 Key）
- [ ] 安装包端到端实测（上传 + 设置 + 提取）

#### 打包产物

| 类型 | 路径 | 大小 |
|------|------|------|
| 安装包 | `release/ResearchAssistant Setup 1.0.1.exe` | 127.93 MB |
| 便携版 | `release/win-unpacked/ResearchAssistant.exe` | 191.91 MB（需整目录） |

#### 遗留问题

| 问题 | 优先级 | 说明 |
|------|--------|------|
| DeepSeek 真实连接 | P1 | 需用户申请 Key 后验证 `api.deepseek.com` |
| 安装包端到端回归 | P1 | 上传、设置、提取、Provider 切换 |
| Alembic 路径缺失 | P2 | dev 启动报 `Path doesn't exist: alembic`，fallback 可用 |
| kimi-k2.6 权限 | P2 | 列表已收录，Tier1 是否可用待用户实测 |

#### 明日计划
1. 安装 v1.0.1 安装包，端到端验证 PDF 上传与 Moonshot 128k 提取
2. 如有 DeepSeek Key，补测 DeepSeek Provider 真实连接
3. 修订 SPEC v1.1.0 并启动项目管理后端改造

---

### 2026-06-11（周四）

#### 今日目标
- [x] 审阅并修正 `ResearchAssistant_SPEC_v1.1.0.md`（基线对照、迁移方案、API 标注）
- [x] v1.1.0 Step 1 后端：`batches` → `projects` 迁移与 API
- [x] 文献 API 增量：批量删除、按项目筛选、移动项目
- [x] pytest 全量回归

#### 执行记录

| 时间 | 任务 | 内容摘要 | 产出 |
|------|------|----------|------|
| 上午 | SPEC 修正 | 增加 v1.0.1/v1.1.0 基线对照表；统一 electron-updater 更新方案；补 §5.4 迁移方案；API 章节标注实现状态 | `ResearchAssistant_SPEC_v1.1.0.md` |
| 上午 | SPEC 替换 | 删除旧 `ResearchAssistant_SPEC.md`，以 v1.1.0 版为唯一规格文档 | 文档整理 |
| 下午 | Alembic 迁移 | `batches`→`projects`（`is_default`、默认「未分类」）；`papers.project_id`；`extracted_data` 六字段扩展 | revision `b2c3d4e5f6a7` |
| 下午 | projects 模块 | 新建 `projects.py`：CRUD、move-papers、`/{id}/batch-extract`；下线 `/api/v1/batches` 路由 | `backend/api/v1/projects.py` |
| 下午 | literature 改造 | 上传/列表支持 `project_id`；`POST /batch-delete`；`PUT /{id}/project`；stats 增加 `by_project` | `literature_crud.py` |
| 晚间 | 测试与迁移验证 | pytest 109 通过；开发库 Alembic 升级至 head；`a1b2` 迁移幂等化 | commit `9d2c387` |

#### 关键问题与决策

| 问题 | 决策 | 影响范围 | 后续跟进 |
|------|------|----------|----------|
| SPEC 目标态与 v1.0.1 代码混写易误导 | 文首增加基线对照表 + API `[v1.0.1 已有]` / `[v1.1.0 新增]` 标注 | 全部开发指令 | Step 2 前端按标注动工 |
| SQLite 改 `papers` 列触发 FTS 失败 | 迁移前 DROP 六个 FTS 触发器，完成后重建 | Alembic `b2c3d4e5f6a7` | 生产升级需走 Alembic |
| 删项目后文献归属 | 文献移入 `is_default=true` 的「未分类」，不删文献 | `DELETE /projects/{id}` | 与 SPEC §4.1 一致 |
| `batch_id` 兼容 | 上传/列表/问答保留 `batch_id` 别名，内部统一 `project_id` | 前后端过渡期 | Step 2 前端全面改 `project_id` |

#### 代码变更清单

```
新增：
  - ResearchAssistant_SPEC_v1.1.0.md
  - backend/api/v1/projects.py
  - backend/services/project_service.py
  - backend/alembic/versions/b2c3d4e5f6a7_projects_and_extracted_fields.py
  - backend/tests/test_projects.py
修改：
  - backend/models/tables.py（Project、project_id、extracted_data 新字段）
  - backend/models/schemas.py（Project schemas、batch-delete、by_project）
  - backend/api/v1/literature_crud.py
  - backend/api/v1/batch_extract.py（逻辑模块 + projects 路由挂载）
  - backend/api/v1/literature_chat.py
  - backend/main.py
  - backend/services/api_provider.py（Kimi Coding 默认 kimi-k2.6）
  - backend/tests/conftest.py、test_batch_extract.py、test_batches.py（跳过）
删除：
  - ResearchAssistant_SPEC.md（由 v1.1.0 替代）
```

#### 验收结果
- [x] Alembic `b2c3d4e5f6a7` 在开发库执行成功
- [x] 默认项目「未分类」自动创建，`is_default` 不可删/不可改名
- [x] 删项目后文献移入未分类，`moved_count` 正确
- [x] `POST /literature/batch-delete` 级联清理 PDF + DB
- [x] pytest 全量 **109 passed**（10 skipped 为遗留 batches 测试）
- [x] Git 提交（commit hash: `9d2c387`）

#### 遗留问题

| 问题 | 优先级 | 说明 |
|------|--------|------|
| Step 2 前端未动工 | P0 | Projects 页、删除入口、上传选项目 |
| `chat_sessions.project_id` | P2 | SPEC 有字段，Step 1 未迁移 |
| v1.1.0 安装包未打 | P1 | 需 Step 2+ 完成后 bump 版本打包 |
| DeepSeek 真实连接 | P1 | 仍待用户 Key |

#### 明日计划
1. Step 2 前端：Projects 页 + 文献删除/移动项目 UI
2. 安装包端到端回归（v1.0.1 或 v1.1.0 预览）
3. Step 4 Prompt 工程化（extract_prompt 新字段写入）

---

### 2026-06-12（周五）

#### 今日目标
- [x] v1.1.0 Step 2 前端：Projects 页、文献删除、项目筛选、上传选项目
- [x] 版本号升至 1.1.0 并提交 Git
- [x] 生产打包 v1.1.0 安装包（前端 build + PyInstaller + electron-builder）

#### 执行记录

| 时间 | 任务 | 内容摘要 | 产出 |
|------|------|----------|------|
| 上午 | Step 2 前端适配 | `batch`→`project` 全局替换；新增 `Projects.tsx`（CRUD + 删除移入未分类）；文献列表单篇/批量删除 + 二次确认；上传选项目；Settings Kimi Coding 默认 `kimi-k2.6` | commit `a6da3a7` |
| 上午 | 路由与导航 | `/projects` 路由；侧栏「项目」入口；文献库 `?project_id=` 筛选 | `App.tsx`、`Layout.tsx`、`Literature.tsx` |
| 下午 | 版本号 | `package.json`、`backend/config.py` → **1.1.0** | commit `591a1b5` |
| 下午 | 生产构建 | 前端 `npm run build`；PyInstaller `backend.spec`；`npx electron-builder` | `release/ResearchAssistant Setup 1.1.0.exe`（127.95 MB） |

#### 关键问题与决策

| 问题 | 决策 | 影响范围 | 后续跟进 |
|------|------|----------|----------|
| `/api/v1/batches` 已下线 | 前端统一改用 `/api/v1/projects` 与 `project_id` | 全部前端 API 调用 | 旧版安装包升级需走 Alembic |
| 删除文献需二次确认 | 单篇「…」菜单与批量「删除选中」均弹 Dialog 确认 | LiteratureList | — |
| Kimi Coding 默认模型 | 前端 `defaultModelForProvider('kimi-coding')` 改为 `kimi-k2.6`，与后端 `api_provider.py` 对齐 | Settings 页 | 安装包实测 |
| 打包前开发进程 | 确认 5173/8000/8084 无占用后构建，避免资源锁冲突 | 打包流程 | — |

#### 代码变更清单

```
新增：
  - frontend/src/pages/Projects.tsx（项目列表/创建/编辑/删除）
修改：
  - frontend/src/types/index.ts（Project 类型、project_id、project_name）
  - frontend/src/pages/Literature.tsx（projects API、URL 项目筛选）
  - frontend/src/components/LiteratureList.tsx（项目列、筛选、删除）
  - frontend/src/components/FileUploader.tsx（上传选项目）
  - frontend/src/components/Layout.tsx（侧栏「项目」）
  - frontend/src/App.tsx（/projects 路由）
  - frontend/src/pages/Settings.tsx（kimi-k2.6 默认）
  - package.json（version 1.1.0）
  - backend/config.py（VERSION 1.1.0）
删除：
  - （无）
```

#### 验收结果
- [x] 前端 `npm run build` 通过（tsc + vite，无错误）
- [x] PyInstaller 后端打包成功（`dist/backend/`）
- [x] electron-builder 生成 NSIS 安装包与 win-unpacked
- [x] Git 提交 Step 2 + 版本号（`a6da3a7`、`591a1b5`）
- [ ] 安装包端到端实测（项目 CRUD、上传、删除、提取、设置）

#### 打包产物

| 类型 | 路径 | 大小 |
|------|------|------|
| 安装包 | `release/ResearchAssistant Setup 1.1.0.exe` | 127.95 MB |
| 便携版 | `release/win-unpacked/ResearchAssistant.exe` | 191.91 MB（需整目录） |

#### 遗留问题

| 问题 | 优先级 | 说明 |
|------|--------|------|
| v1.1.0 安装包端到端回归 | P0 | 项目/文献/删除/设置需在桌面 App 验证 |
| `chat_sessions.project_id` | P2 | SPEC 有字段，后端 Step 1 未迁移 |
| DeepSeek 真实连接 | P1 | 仍待用户 Key |
| Step 4 Prompt 工程化 | P2 | extract_prompt 新字段写入 |

#### 明日计划
1. 安装 `ResearchAssistant Setup 1.1.0.exe`，端到端验证 v1.1.0 全功能
2. 根据实测反馈修复 P0 问题并视情况打补丁包
3. 推进 Step 4 Prompt 工程化或论文撰写模块

---

### 2026-06-12（周五 · 补充）

#### 今日目标
- [x] 生成 v1.1.0 → v1.2.0 代码基线审阅报告
- [x] 生成 v1.1.0 → v1.2.0 开发进度规划 2.0
- [x] 将审阅报告加入 .gitignore，提交其余改动并更新项目日志

#### 执行记录

| 时间 | 任务 | 内容摘要 | 产出 |
|------|------|----------|------|
| 下午 | 基线审阅 | 对照 SPEC v1.2.0 逐模块审阅后端/前端/DB/迁移，输出差距矩阵 16 项、债务清单 7 项、Phase 0-4 路线 | `审阅报告.md` |
| 下午 | 进度规划 2.0 | 基于审阅报告排序 Phase 路线：止血→加固→Job化→清理→增强，明确并行/串行关系与双人分工 | `开发进度规划2.0.md` |
| 下午 | 收尾提交 | `审阅报告.md` 入 .gitignore；提交 `开发进度规划2.0.md` + `.gitignore` + `项目日志.md` | commit |

#### 关键问题与决策

| 问题 | 决策 | 影响范围 | 后续跟进 |
|------|------|----------|----------|
| v1.1.0 新字段（research_question 等 6 列）全链路断链 | 上提为 Phase 0 最高优先级——先让 prompt 产出新字段、再接通提取/问答/响应，否则 ADR-2 质量契约无字段可校验 | Phase 0-3 全部规划 | 下一个开发周期从 Phase 0 ★首 commit 启动 |
| 审阅报告含敏感分析细节 | 纳入 .gitignore，仅本地保留 | 仓库公开范围 | — |

#### 代码变更清单

```
新增：
  - 审阅报告.md（.gitignore 排除）
  - 开发进度规划2.0.md
修改：
  - .gitignore（新增 审阅报告.md）
  - 项目日志.md（追加本节）
删除：
  - （无）
```

#### 验收结果
- [x] 审阅报告 16 项差距矩阵 + 7 项债务清单 + Phase 0-4 路线完整
- [x] 进度规划 2.0 含 ★首 commit 明确到文件与代码片段
- [x] `审阅报告.md` 已加入 .gitignore
- [x] Git 提交

#### 明日计划
1. 安装 v1.1.0 安装包，端到端实测
2. 启动 Phase 0 ★首 commit：`lifespan` 启动重置 `extracting→pending`
3. 推进 Phase 0.2-0.4：新字段最小闭环

---

### 2026-06-15（周一）

#### 今日目标
- [x] M5.2 Stage 0：Buchholz2009 原子论断抽取（DeepSeek V4 Flash，70→40 条）
- [x] 务实挂起 quote 逐字问题，登记 F17 技术债
- [x] 34 条 claims 写入数据库，修复 DB schema 漂移
- [x] 修复 `AlignmentResult` JSON 序列化 bug

#### 执行记录

| 时间 | 任务 | 内容摘要 | 产出 |
|------|------|----------|------|
| 下午 | 论断抽取 | `_extract_claims.py` 对 Buchholz2009 全量 7 块并发抽取，分块→DeepSeek Flash→去重→三层对齐→回灌重抽，最终 40 条 | `_claims_workdir/claims_cache.json` |
| 下午 | F17 登记 | Quote 通过率 ~15-18%，核实为 LLM 本质缺陷（Flash/Pro 均轻微改写），决定挂起、标记 low_confidence 不阻塞局限聚类 | `审阅报告2.md` §2.2 |
| 下午 | AlignmentResult 修复 | `asdict` 导入 + json.dump 前转换，解决 NamedTuple 不可序列化 | `_extract_claims.py:1479` |
| 下午 | DB 重建 | 旧 DB 有 schema 漂移（`batch_id` 未 rename），删除重建 + `alembic stamp head`；创建 paper 记录 + 写入 34 条 claims | `_write_claims_v2.py` |
| 下午 | 写库验证 | 抽样 5 条验证 `claim_form`/`topic`/`is_limitation`/`quote` 字段正确 | — |

#### 关键问题与决策

| 问题 | 决策 | 影响范围 | 后续跟进 |
|------|------|----------|----------|
| Quote 逐字校验通过率仅 15% | 务实挂起：quote 标记 low_confidence，不阻塞局限聚类；根治方案为 anchor 抠取架构（不在本期） | claims.quote 字段精度 | 局限性聚类跑通后，人在信号面板核对升级 |
| DB schema 漂移（`batch_id`→`project_id` 迁移残留） | 删除空库重建，`Base.metadata.create_all` + `alembic stamp head` | 本地开发环境 | 生产升级需走 Alembic migration |
| `AlignmentResult` NamedTuple JSON 不可序列化 | 导入 `dataclasses.asdict`，写缓存前转 dict | `_extract_claims.py` 输出 | 已修复 |

#### 代码变更清单

```
新增：
  - ResearchAssistant_SPEC_v1_3_0.md（v1.3.0 规格文档）
  - 开发进度规划2.1.md（基于 v1.3.0 的进度规划更新）
  - _claims_workdir/_write_claims_v2.py（claims 写库脚本）
修改：
  - .gitignore（+ .env.local, 审阅报告2.md, _claims_workdir/, _extract_*.txt, _test_*.py）
  - 审阅报告2.md（F17 登记）
  - backend/_extract_claims.py（asdict 导入 + AlignmentResult 序列化修复）
删除：
  - data/research-assistant.db（重建）
  - _claims_workdir/_check_db.py, _fix_fk.py, _write_claims.py（临时脚本已清理）
```

#### 验收结果
- [x] Buchholz2009 论断抽取完成：70→40 条（去重 + 对齐 + 重抽后）
- [x] F17 技术债登记完整（现象/根因/根治方案/当前处理/影响范围）
- [x] 34 条 claims 写入 claims 表，paper_id 正确关联
- [x] 抽样 5 条字段验证通过
- [x] `AlignmentResult` 序列化 bug 已修复
- [x] `.gitignore` 覆盖全部临时文件

#### 遗留问题

| 问题 | 优先级 | 说明 |
|------|--------|------|
| Quote 逐字可追溯性未达标 | P2（挂起） | F17，根治为 anchor 架构，不在本期 |
| 局限聚类信号待跑通 | P0 | Phase 2 下一步，需 claims 数据就绪 |
| `_extract_claims.py` 为 untracked 工具脚本 | P2 | `backend/_*.py` 被 gitignore 排除，修复未入库 |

#### 明日计划
1. 启动局限聚类（基于已入库的 34 条 claims）
2. 如局限聚类产出信号，接入信号面板
3. 继续 v1.3.0 Phase 1 其余论文论断抽取

---

### 2026-06-16（周二）

#### 今日目标
- [x] M5.2 收尾：登记里程碑、更新 F17/F18 审阅状态
- [x] M5.2 扩展：Zhang2021 + Iwama2024 论断抽取 + quote_locator 定位

#### 执行记录

| 时间 | 任务 | 内容摘要 | 产出 |
|------|------|----------|------|
| 上午 | M5.2 里程碑登记 | 登记"论断抽取链路打通(quote 层次一完成)"：claims schema 落库、Flash 抽取 ~20s/篇、分类质量验证可用、quote_locator 独立模块（整句模糊+硬锚点）、删除旧三层对齐+回灌重抽 | 本日志 |
| 上午 | F17/F18 更新 | 审阅报告2：F17→层次一完成、F18(PDF提取质量全局债)登记 | `审阅报告2.md` |
| 上午 | Zhang2021 抽取 | 7块并发，17.9s（API 15.2s），47条，18 verified(38%)，safety:13/efficacy:19 | `_claims_workdir/Zhang2021_claims_cache.json` |
| 上午 | Iwama2024 抽取 | 2块并发，20.4s（API 19.4s），30条，2 verified(7%)，efficacy:16/safety:8，分块覆盖仅 47% | `_claims_workdir/Iwama2024_claims_cache.json` |

#### 关键问题与决策

| 问题 | 决策 | 影响范围 | 后续跟进 |
|------|------|----------|----------|
| M5.2 论断提取链路 | 封存为层次一完成：claims 落库、抽取、分类、quote_locator 独立模块均就绪 | M5.2 | F17/F18 为已知债 |
| F17 quote 逐字率 29% | 不阻塞 M5.2 封存；根因 PDF 提取质量（F18），非算法问题 | quote 精度 | F18 为下一轮重点 |

#### 遗留问题

| 问题 | 优先级 | 说明 |
|------|--------|------|
| F17 | P2（挂起） | quote verified 率 29%，受 PDF 提取质量限制 |
| F18（新增） | P1（下一轮重点） | PDF 提取质量全局债：词粘连(integipsc)、连字符换行、特殊字符丢失，影响 quote_locator 天花板 |
| Zhang2021 + Iwama2024 分类质量 | P1 | 待用户原文核对 safety/outcome 类准确性 |

---

## 附录：快速索引

### 2026-06-17（周三）

#### 今日目标
- [x] Step 4 局限聚类 API 化：subject 透传 + 聚类服务 + API 端点 + 实测
- [x] 整理并提交之前未入库的 claims 提取后台 Job 化代码（9 文件）

#### 执行记录

| 时间 | 任务 | 内容摘要 | 产出 |
|------|------|----------|------|
| 下午 | subject 透传 | ClaimRecord 数据类新增 `subject` 字段；`fetch_limitation_claims` 通过 ORM 读取 claim.subject；evidence 输出逐条含 `subject`，AD-7 命脉字段全程透传 | `limitation_clusterer.py` |
| 下午 | 聚类核心服务 | 新建 397 行，搬入 `_run_clustering.py` 的 GROUPING_PROMPT / group_one_topic / _make_solo_group / 分桶逻辑；泛化接受 project_id，删 7 处硬编码；AI 调用改用 KimiClient | `limitation_clusterer.py` |
| 下午 | 聚类 API 端点 | `POST /api/v1/projects/{project_id}/clustering/limitation`，同步执行 28s 超时，慢模型返回 504 + 明确切换提示；Pydantic 响应模型（CandidateGroupItem / EvidenceItem / AdjudicationBlock），本步不落库 | `claims.py` (+888 行) |
| 下午 | 实测 | 4 篇 RPE 文献 21 条 claims → 11 候选组（1 跨论文组）；subject 透传 21/21 非空；ADR-7 合规（无 signal_name/confidence）；聚类质量肉眼可接受 | 实测 JSON |
| 下午 | HTTP API 验证 | 路由注册确认，慢模型超时返回 504 正常，错误提示明确 | API 测试 |
| 傍晚 | 工作区整理 | 发现 3 modified + 6 untracked 文件（ExtractJob 表 + worker + chat_json + 3 迁移），全部属于 claims 提取后台 Job 化功能，打包提交 | commit `e898c5c` |
| 傍晚 | 提交 Step 4 | `feat: 局限聚类API化(Step4) — subject透传 + 聚类服务 + API端点` | commit `96f001e` |

#### 关键问题与决策

| 问题 | 决策 | 影响范围 | 后续跟进 |
|------|------|----------|----------|
| ClaimRecord 缺 subject 字段导致聚类丢失研究对象 | 数据类 + DB 读取 + evidence 输出三处补全 subject，实测 21/21 非空 | 局限聚类、裁决面板 | Step 5 落库时 subject 继续透传 |
| `_run_clustering.py` 硬编码 DB 路径 / DeepSeek key / 论文名 | 重写为 ORM 读取 + KimiClient 统一调用 + project_id 参数化，删 7 处硬编码 | limitation_clusterer.py | — |
| 慢模型（deepseek-v4-flash）聚类耗时 284s，远超 30s 目标 | 28s 超时 + 504 明确提示切换更快模型（如 kimi-k2.6 / moonshot-v1-8k） | API 可靠性 | 用户切换模型后验证 30s 内完成 |
| 聚类输出必须 ADR-7 合规 | 候选组 + evidence，adjudication= pending/null，无 signal_name/confidence，grouping_method + grouping_basis 必填 | 裁决面板数据契约 | Step 5 落库时保持结构 |
| 之前 claims 提取 Job 化代码（9 文件）未提交 | 确认功能完整（ExtractJob 表 + worker + chat_json + 3 迁移），打包为单次提交 | main.py / tables.py / kimi_client.py | 后续 Step 的 worker 可复用此架构 |

#### 代码变更清单

```
新增：
  - backend/services/limitation_clusterer.py（397 行，局限聚类核心服务）
  - backend/alembic/versions/f6g7h8i9j0k1_add_extract_jobs.py（ExtractJob 迁移）
  - backend/alembic/versions/g7h8i9j0k1l2_add_quote_hash_to_claims.py（quote_hash 迁移）
  - backend/alembic/versions/h8i9j0k1l2m3_add_claims_unique_constraint.py（唯一约束迁移）
  - backend/services/claim_extract_prompt.py（311 行，提取 prompt）
  - backend/services/claim_extractor.py（535 行，提取逻辑）
  - backend/services/extract_job_worker.py（616 行，后台 worker）
修改：
  - backend/api/v1/claims.py（+888 行，聚类 API + Pydantic 模型）
  - backend/main.py（+43/-6，ExtractJob 僵尸重置 + claims worker 生命周期）
  - backend/models/tables.py（+78，ExtractJob 表 + JobStatus 枚举）
  - backend/services/kimi_client.py（+63，chat_json 方法 + get_model_name）
删除：
  - （无）
```

#### 验收结果
- [x] subject 透传：21/21 evidence 含真实生物学对象值（hiPSC-derived RPE cells 等）
- [x] 聚类 API 对 4 篇 RPE 文献聚出 11 候选组（含 1 跨论文组 safety）
- [x] ADR-7 合规：无 signal_name/confidence，adjudication=pending
- [x] HTTP API 路由注册正确（`POST .../clustering/limitation`）
- [x] 超时处理：慢模型 28s 返回 504 + 明确提示
- [x] 聚类质量肉眼检查：G2「移植细胞定位与分布」3 条同主题聚合正确；G11「安全性评估方法局限」跨 2 篇论文聚合正确
- [x] 工作区干净，无残留临时文件
- [x] Git 提交 2 次（`96f001e`、`e898c5c`）

#### 遗留问题

| 问题 | 优先级 | 说明 |
|------|--------|------|
| 慢模型下聚类超 30s | P2 | deepseek-v4-flash 需 284s，需用户切换 kimi-k2.6 等快模型后验证 |
| 聚类结果未落库 | — | Step 5 待实现（候选组 + evidence 写入 DB） |
| 跨论文组仅 1 个 | P2 | 当前 21 条 claims 的 topic 分布分散，更多论文入库后跨论文组会增加 |

#### 明日计划
1. Step 5：聚类结果落库（候选组表 + evidence 关联）
2. 裁决面板 API：读取候选组、人工裁决（确认/驳回/合并）
3. 如时间允许，推进 F18 PDF 提取质量改进

---

### 2026-06-22 ~ 06-25（发布冲刺：1.2.1 → 1.2.2 → 1.2.3）

> 本节为"下一会话可接手"的交接形态。基线 commit `911f8f9`（v1.2.0），本阶段两笔归档提交：`8155ce5`(1.2.2) → `a535b50`(1.2.3)。

#### 版本历程（本阶段做了什么）

| 版本 | 主题 | 关键改动 |
|------|------|----------|
| **1.2.1** | 模型默认值修复（moonshot→flash） | `settings.py` 默认 Provider 改 DeepSeek；`literature_chat.py`/`literature_translate.py` 由硬编码 `DEFAULT_MODEL` 改用 `client._model`，问答/翻译实际发出 `deepseek-v4-flash`。（未单独打包/提交，随 1.2.2 一并归档进 commit `8155ce5`） |
| **1.2.2** | 提取流水线可靠性整治（P1–P6） | P1 放宽摘要校验（`schemas.py` 契约由"必填非空"→"尽力而为"，`extract_service.py` 增 `ParseResult.partial/missing_fields`，仅完全空响应/不可解析 JSON 判失败）；P2 失败不锁死（`literature_extract.py` 不再写 `EXTRACT_FAILED`，转 `PENDING` 可自动重提；`kimi_client.py` 增 `is_retryable_error/retry_after_seconds` 区分可重试与真失败）；P3 worker 自愈（`extract_job_worker.py` 异常/取消收尾置 FAILED/INTERRUPTED + `JOB_STALE_TIMEOUT` 回收僵尸；`claims.py` 并发拦截排除孤儿）；P4 claims 可见（`literature_crud.py` 带出 `claims_count`；`claims.py` 新增 `GET /claims/by-paper/{id}`；前端 List/Detail 展示）；P5 保守并发（`config.py` `batch_extract_concurrency=3`+Semaphore；`db.py` `busy_timeout=15000`）；P6 上传归属（`FileUploader.tsx` `defaultProjectId` 按选中项目落库）。安装包：`release/ResearchAssistant Setup 1.2.2.exe`。 |
| **1.2.3** | 信号发现入口统一占位"敬请期待" | 新增 `components/ComingSoon.tsx`（占位组件 + `COMING_SOON_MESSAGE`）；`App.tsx` 把 `/adjudication` 路由改渲染 `ComingSoon`；`Literature.tsx` "分析局限"按钮去掉 `/clustering/limitation` 原生 fetch、改弹 toast，并移除聚类状态/进度条。**仅前端拦截，后端聚类/裁决代码与路由全部保留。** 安装包：`release/ResearchAssistant Setup 1.2.3.exe`（包内 backend.exe MD5 `EE6A89E4DC73326655C5DE1CBBCC8428`，与源 dist 一致；前端 bundle 内含"敬请期待"已核）。 |

#### 当前已发布功能 vs 暂挡功能

| 状态 | 功能 |
|------|------|
| ✅ 已发布 | 文献上传/项目管理与归属、PDF 阅读、AI 摘要提取（部分成功入库、失败不锁死）、批量 Claims 提取（保守并发）、Claims 结果在文献库列表/详情可见、单篇精读问答、跨文献问答、摘要翻译、设置（多 Provider，默认 DeepSeek flash） |
| ⏸️ 暂挡（占位"敬请期待"） | 信号发现 / 机会矩阵模块：分析局限（局限聚类）、裁决面板（裁决/信号），以及 SPEC 规划但前端尚无入口的 矛盾点 / 稀疏格 / 方法迁移 |

#### 下一步主线（接手重点）

1. **信号发现核心功能"复活"（成本可能很低）**
   - 后端 `POST /api/v1/projects/{id}/clustering/limitation` **已实现并注册**（`backend/api/v1/claims.py:906`，router 前缀 `/api/v1/projects` 见 `claims.py:55`，挂载于 `main.py:198`）；聚类服务 `backend/services/limitation_clusterer.py` + 落库 `cluster_writer.py` 均在。裁决 API `backend/api/v1/adjudication.py` 同样已实现并注册（`main.py:197`）。
   - "分析局限"点击报 **Failed to fetch 的真因不是后端缺失，而是前端 `frontend/src/pages/Literature.tsx:235`（旧代码）用了原生相对 `fetch('/api/v1/...')` 未走 `resolveApiBase()`**；装机版前端是 `file://` 源，相对地址解析不到 `http://127.0.0.1:8000`，请求未送达。（对比：`apiGet/apiPost` 会自动拼后端 base，故其它功能正常。）
   - **复活路径**：把 `Literature.tsx` 的聚类调用改为 `apiPost('/projects/{id}/clustering/limitation')`（走 base），并把 `App.tsx` 的 `/adjudication` 路由从 `ComingSoon` 换回 `<Adjudication />`（`frontend/src/pages/Adjudication.tsx` 仍保留）。注意慢模型（deepseek-v4-flash）聚类约 284s 会触发 28s 超时→504，需引导切快模型（kimi-k2.6 等）或改异步 Job 化。
2. **下游功能"改读全文"的 ADR**
   - 仓库暂无该专项 ADR 文件（`docs/` 下仅 API_CONTRACT/ERROR_HANDLING/KNOWN_ISSUES/SECURITY；SPEC 为 v1.1.0/v1.2.0/v1.3.0）。**待办**：新建 `docs/ADR-下游改读全文.md`，记录"摘要/claims/问答下游是否改为读全文而非读摘要"的取舍与迁移方案（本轮明确不做）。

#### 交接形态（接手须知）

- **git**：分支 `master`，HEAD=`a535b50`(1.2.3) ← `8155ce5`(1.2.2) ← `911f8f9`(1.2.0 基线)。**两笔均未 push**（按要求等确认）。`backend/config.py` 的 `VERSION` 仍为 `1.2.0`（历史遗留，安装包内后端启动日志因此显示 v1.2.0；如需对齐可后续单独 bump）。
- **安装包**：当前发布版 `release/ResearchAssistant Setup 1.2.3.exe`；回退版 `release/ResearchAssistant Setup 1.2.2.exe`。更旧的 1.2.1/1.2.0 及打包中间产物清理方案见当次"工作区整理"对话（待确认后删）。
- **后台/数据**：本机暂无装机版生产 DB（仅 `%TEMP%\ra_test_*` 测试库）；开发库在 `data/research-assistant.db`。
- **测试**：后端套件（排除需活服务的 `tests/test_adjudication.py`）全绿；前端 `npm run build` 通过。

---

### 里程碑状态速查

| Milestone | 名称 | 计划日期 | 实际完成 | 状态 |
|-----------|------|----------|----------|------|
| M1 | 项目脚手架 | Week 1 Day 1-2 | — | ✅ |
| M2 | 数据层 | Week 1 Day 2-4 | — | ✅ |
| M3 | 基础服务 | Week 1 Day 4-6 | — | ✅ |
| M4 | 文献 CRUD | Week 1 Day 6-9 | 2026-06-05 | ✅ |
| M5 | 核心 AI 链路 | Week 2 Day 9-14 | 2026-06-06 | ✅ |
| M5.2 | 论断抽取链路打通（层次一） | — | 2026-06-16 | ✅ |
| M6 | 辅助模块 | Week 2 Day 14-16 | 2026-06-07 | ✅ |
| M7 | 后端验收 | Week 2 Day 16-18 | 2026-06-08 | ✅ |
| M8 | Electron 壳 | Week 3 Day 18-20 | — | ⬜ |
| M9 | React 骨架 | Week 3 Day 20-22 | — | ⬜ |
| M10 | 核心组件 | Week 3-4 Day 22-28 | 2026-06-08 | ✅ |
| M11 | 功能页面 | Week 4 Day 28-31 | 2026-06-12 | ✅ |
| M12 | 前端验收 | Week 4 Day 31-32 | — | 🟡（待安装包实测） |
| v1.1.0 Step 1 | 项目管理后端 | — | 2026-06-11 | ✅ |
| v1.1.0 Step 2 | 项目管理前端 | — | 2026-06-12 | ✅ |
| Step 3 | 论文撰写 | Week 5 | — | ⬜ |
| Step 4 | 打包发布 | Week 6 | 2026-06-12（v1.1.0 安装包） | 🟡（待 App 实测） |

### 里程碑 M5.2 详情

**M5.2 — 论断抽取链路打通（quote 层次一完成）** | 2026-06-16封存

| 条目 | 内容 |
|------|------|
| claims schema 落库 | claims 表 11 字段（claim_form/topic/direction/comparison_result/magnitude/context/stat_support/is_limitation/quote/quote_page/notes） |
| Flash 抽取 | DeepSeek V4 Flash 分块并发，~20s/篇 |
| 分类质量验证可用 | claim_form 四分类 + topic 五选一 + is_limitation 提取经 Buchholz2009 原文核对通过 |
| quote_locator 独立模块 | 整句模糊定位 + 硬锚点；verified 逐字 / unverified 诚实 / 负例不张冠李戴 |
| 删除旧架构 | 三层对齐 + 回灌重抽 已下线 |
| **已知债** | **F17**：verified 率 29% 受限 PDF 提取质量（非算法问题）；**F18**(新增)：PDF 提取质量全局债（词粘连/连字符换行/特殊字符丢失），下一轮重点 |

### 阻塞问题跟踪

| 日期 | 问题描述 | 状态 | 解决日期 | 解决方案 |
|------|----------|------|----------|----------|
| — | — | — | — | — |

### 接口契约冻结记录

| API | 冻结日期 | 验证方式 | 变更历史 |
|-----|----------|----------|----------|
| POST /upload | — | Postman | — |
| POST /{id}/extract | — | Postman + SSE | — |
| POST /chat | — | Postman + SSE | — |
| POST /{id}/chat | — | Postman + SSE | — |
| GET /settings | — | Postman | — |
| PUT /settings | — | Postman | — |
