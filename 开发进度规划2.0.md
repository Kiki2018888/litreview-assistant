# ResearchAssistant 开发进度规划 2.0（v1.1.0 → v1.2.0）

> 配套文档：`审阅报告.md`（差距矩阵与债务清单）、`ResearchAssistant_SPEC_v1_2_0.md`（目标架构）
> 起点：v1.1.0 基线（HEAD `2cdb74f`，2026-06-12）
> 排序原则：**先止血 → 再加固 → 后增强**；数据正确性 > 可靠性 > 增强能力
> 关键判断：v1.1 已加的"新字段"在提取/问答/响应三层断链（差距矩阵 G2），是 ADR-2 的地基，**上提为 Phase 0/1 前置**。

---

## 总览：Phase 路线与依赖

```
Phase 0 止血 ──→ Phase 1 加固提取(ADR-2) ──→ Phase 3 收尾(DROP旧列) ──→ Phase 4 增强(v1.2)
   │                    │                            ↑
   │                    └────────────────────────────┘ (新字段全链路接通是 DROP 旧列的前置)
   │
   ├─(并行)→ ADR-4 WAL/busy_timeout 配置 ────────────┐
   ├─(并行)→ Phase 3a paper_blocks+project_id ───────┤
   └─(并行)→ 前端"按项目问答"(后端已支持) ────────────┘
                                                     ↓
Phase 2 批量提取 Job 化(ADR-3) + DB访问统一(ADR-4 重构部分)
```

| Phase | 主题 | ADR | 预估 | 串/并 |
|-------|------|-----|------|-------|
| 0 | 止血：启动重置 + 新字段最小闭环 + 契约冻结 | ADR-3(子集) | 1-2d | 主线起点 |
| 1 | 加固提取：质量契约 | ADR-2 | 2-3d | 串行（依赖 P0 新字段）|
| 2 | 批量提取 Job 化 + DB 访问统一 | ADR-3, ADR-4 | 3-5d | 串行（提取主线）|
| 3 | 清理结构债 + DROP 旧列 | — | 2-3d | 部分并行 |
| 4 | 能力增强：检索 + 成本 | ADR-1, ADR-5 | 按需 | 增强 |

---

## ★ 最先动手的一个 commit

**`feat(lifespan): 启动时重置残留 extracting→pending`**

- **文件**：`backend/main.py` → `lifespan()`，在 `_run_migrations()` 之后、`yield` 之前插入一次性重置。
- **改动**（约 10 行）：
  ```python
  # 启动重置：消除上次异常退出残留的 extracting 僵尸态（ADR-3 状态机子集）
  from backend.services.db import SessionLocal
  from backend.models.tables import Paper, PaperStatus
  _db = SessionLocal()
  try:
      n = _db.query(Paper).filter(Paper.status == PaperStatus.EXTRACTING.value).update(
          {Paper.status: PaperStatus.PENDING.value, Paper.last_error: "服务重启，提取中断"},
          synchronize_session=False,
      )
      _db.commit()
      if n:
          logger.info("启动重置：%d 篇 extracting → pending", n)
  finally:
      _db.close()
  ```
- **为什么是它**：零迁移、零接口变更、不依赖任何其他 Phase；直接消灭 SPEC §12"批量 Job 中断遗留僵尸文献（高风险）"；是 ADR-3 状态机的最小可交付子集。
- **验收**：手动 `UPDATE papers SET status='extracting'` → 重启后端 → 该文献回到 `pending`。
- **遗留**：`extract_jobs` 的 `running→interrupted` 待 Phase 2 建表后补。

---

## Phase 0 · 止血

**目标**：消除数据正确性隐患与僵尸态，冻结契约，确认 v1.1.0 安装包可用。

| 任务 | 涉及文件 | 验收标准 | 风险点 |
|------|----------|----------|--------|
| 0.1 启动重置 extracting→pending（★首 commit）| `backend/main.py` | 重启后无残留 extracting | 无 |
| 0.2 提取 Prompt 产出新字段 | `backend/services/extract_prompt.py` | prompt 索取 research_question/sample_source/sample_size/key_methods/key_data/limitations | 影响既有质量，保留 raw_json 可回溯 |
| 0.3 单篇提取写新字段（与 batch 对齐） | `backend/api/v1/literature_extract.py` `_save_extracted_data` | 单篇提取后新列非空 | 与 batch 逻辑去重，建议抽 `services` 公共函数 |
| 0.4 响应暴露新字段 | `backend/models/schemas.py` `ExtractedDataResponse` | 文献详情返回新字段 | 前端 LiteratureDetail 需同步展示（可 Phase 3）|
| 0.5 v1.1.0 安装包端到端回归 | — | 项目 CRUD/上传/删除/提取/设置全通 | 依赖用户桌面实测 |
| 0.6 测试 URL 修正 + 冻结 API_CONTRACT | `backend/tests/*` `docs/API_CONTRACT.md` | pytest 全绿；契约反映 projects 现状 | `test_batches.py` 保持 skip 或删除 |

**并行性**：0.2/0.3/0.4 为新字段闭环，需串行交付（prompt→写→读）；0.5/0.6 可与之并行。

---

## Phase 1 · 加固提取（ADR-2，最高 ROI）

**目标**：`completed` 当且仅当通过 Pydantic schema 校验 + 内容契约。

| 任务 | 涉及文件 | 验收标准 | 风险点 |
|------|----------|----------|--------|
| 1.1 定义提取校验 schema | `backend/models/schemas.py`（新增 `ExtractedDataContract`）| 必填非空 / key_data 含数值（正则）/ limitations≥1 | 数值正则需容忍中英文与单位 |
| 1.2 引入 json-repair 兜底 | `requirements.txt` + 提取解析函数 | JSON 括号/逗号类问题自动修复 | 新依赖打包体积 |
| 1.3 修复循环：校验错误回灌模型 | `literature_extract.py` `batch_extract.py` | 内容不合格走修复而非盲目重跑 | 设修复次数上限，防 token 放大 |
| 1.4 区分两类失败 | 同上 | 调用失败=退避重试；内容不合格=修复循环 | 计数分离（extraction_attempts vs 修复次数）|
| 1.5 质量门槛置 completed | 同上 | 不达标置 extract_failed/pending，记 last_error | — |

**验收（对应 SPEC §13 ADR-2）**：喂格式错乱/字段敷衍样例，系统走修复循环且不把空洞结果置 completed。
**串行依赖**：依赖 Phase 0 新字段已接通（校验对象就是新字段）。
**建议**：把单篇/批量的提取核心逻辑抽到 `backend/services/extract_service.py`，两个端点共用，避免 G2 那种"两份实现各写各的"。

---

## Phase 2 · 批量提取 Job 化 + DB 访问统一（ADR-3 + ADR-4）

**目标**：批量任务持久可靠；并发不锁库。

| 任务 | 涉及文件 | 验收标准 | 风险点 |
|------|----------|----------|--------|
| 2.1 迁移 4：extract_jobs 表 + 索引 | `backend/alembic/versions/<new>.py` `models/tables.py` `schemas.py` | 表+status/project_id 索引建成 | — |
| 2.2 后台 worker | `backend/services/extract_job_worker.py`（新）| 串行执行、间隔≥1s、受 BATCH_EXTRACT_CONCURRENCY 控 | worker 与 Electron 进程生命周期耦合 |
| 2.3 /jobs 端点全套 | `backend/api/v1/jobs.py`（新）`main.py` | 列表/详情/stream订阅/pause/resume/cancel | 路由顺序 |
| 2.4 batch-extract 改返回 job_id | `backend/api/v1/projects.py` | `POST /{id}/batch-extract` 返回 `{job_id,total}` | 前端同步改 |
| 2.5 SSE 退化为订阅 | `jobs.py` 前端 `useSSE.ts` | 断开不影响 Job；重连 GET /jobs/{id} 对齐 | 重连策略区分内容流 vs Job 订阅 |
| 2.6 启动钩子重置中断 Job | `main.py` lifespan | running→interrupted + 关联文献 extracting→pending | 与 0.1 合并完善 |
| 2.7 ★WAL + busy_timeout（可提前并行）| `backend/services/db.py` | 新连接 PRAGMA journal_mode=WAL, busy_timeout=5000 | WAL 文件随 .db 一起备份/导出 |
| 2.8 async DB 访问统一 | 提取/问答/批量端点 | async 端点经 to_thread 调 sync DB | 改动面大，渐进重构 |

**验收（SPEC §13 ADR-3/4）**：提取中关页/重连进度对齐；重启后 Job 可恢复；并发浏览无 `database is locked`。
**并行性**：**2.7 可在 Phase 0/1 期间就并行落地**（仅改 db.py，一行级，立竿见影）；2.8 渐进做。

---

## Phase 3 · 清理结构债

**目标**：多项目隔离 + 旧列退场 + 计数防漂移。

| 任务 | 涉及文件 | 验收标准 | 风险点 |
|------|----------|----------|--------|
| 3a.1 迁移 5：paper_blocks 加 project_id | `alembic/versions/<new>.py` `models/tables.py` | 列+`UNIQUE(project_id, block_name)`；存量归默认项目 | SQLite 改约束需重建表 |
| 3a.2 /paper/blocks 带 project_id | `backend/api/v1/paper.py` `schemas.py` | GET `?project_id=` / PUT body 含 project_id | — |
| 3a.3 前端论文撰写按项目 | `frontend/src/pages/PaperWrite.tsx` | 切项目加载/保存独立草稿 | — |
| 3b.1 chat_sessions 加 project_id | 迁移 + `tables.py` `chat_sessions.py` | 会话可归属项目 | — |
| 3c.1 paper_count 改触发器 | 迁移（CREATE TRIGGER）| INSERT/DELETE/移动自动维护 | 触发器与 ORM 事务交互 |
| 3d.1 收尾迁移：DROP 旧列 | 迁移 + 同步改 FTS5 触发器 + `ExtractedDataResponse` | 全库回填后 DROP background/methods/key_results | **必须确认无依赖**；FTS5 触发器引用旧列需同步重建 |
| 3e.1 LLM 客户端 provider 无关抽象 | `services/kimi_client.py` → 抽象层 | 实现细节，不入契约 | — |

**串行依赖**：3d.1（DROP 旧列）必须在 Phase 0→1 新字段全链路接通 + 全库回填之后。
**并行性**：3a（paper_blocks）/3b（chat_sessions）与提取主线无耦合，可与 Phase 1/2 并行。

---

## Phase 4 · 能力增强（v1.2）

| 任务 | 涉及文件 | ADR | 验收 |
|------|----------|-----|------|
| 4.1 本地 embedding 检索层 | 新 service + paper_pages 向量索引 | ADR-1 | 问"摘要未覆盖"问题能回原文检索作答 |
| 4.2 token_usage 表 + 记录 + 汇总（可提前并行建表）| 迁移 6 + 各调用点 + 统计端点 | ADR-5 | 提取/问答后有 token 记录，项目维度可汇总成本 |
| 4.3 跨文献 map-reduce + 答案缓存 | `literature_chat.py` | — | 大批量问答稳定 |

---

## 并行 / 串行总表

| 任务 | 依赖 | 可并行对象 |
|------|------|-----------|
| 0.1 启动重置 | 无 | 一切 |
| 0.2→0.3→0.4 新字段闭环 | 内部串行 | 0.5/0.6 |
| 2.7 WAL/busy_timeout | 无 | Phase 0/1 全程 |
| 3a paper_blocks+project_id | 无（独立模块）| Phase 1/2 |
| 前端"按项目问答" | 后端已支持 | 任意时点 |
| 1.x 质量契约 | 0.2-0.4 完成 | 3a / 2.7 |
| 2.1-2.6 Job 化 | 1.x（共用提取核心）| 3a/3b |
| 3d DROP 旧列 | 0→1 接通 + 回填 | — |
| 4.2 token_usage 建表 | 无 | 任意 |

**双人分工建议**：
- **A（提取主线）**：★首 commit → 0.2-0.4 → Phase 1 → Phase 2。
- **B（结构债 + 配置 + 前端）**：2.7 WAL → 3a paper_blocks → 前端按项目问答 → 3b/3c → 配合 A 做 3d 收尾。

---

## 验收里程碑（对齐 SPEC §13 架构契约）

- [ ] **M-P0**：重启无残留 extracting；单篇提取写新字段；pytest 全绿。
- [ ] **M-ADR2**：格式错乱样例走修复循环，空洞结果不置 completed。
- [ ] **M-ADR4**：批量提取 + 并发浏览无 `database is locked`。
- [ ] **M-ADR3**：提取中关页/重连进度对齐；杀进程重启 Job 可恢复。
- [ ] **M-P3**：多项目独立草稿；旧列 DROP 后全链路无报错。
- [ ] **M-ADR1（v1.2）**：摘要未覆盖问题能回原文检索作答。
- [ ] **M-ADR5（v1.2）**：token_usage 有记录，项目维度可汇总成本。

---

## 待确认事项（动手前核对，见审阅报告 §5）

1. 开发库 Alembic 是否已到 head `b2c3d4e5f6a7`（`alembic current`）。
2. 当前 pytest 实际通过数（`pytest backend/tests -q`）。
3. `kimi_client.py` 是否已消费 `usage` 字段（影响 4.2 工作量）。
4. 前端批量提取调用点确认（影响 2.4/2.5 前端改动）。
5. `package.json build.publish` owner `youthwei` 是否正确（更新检测 404）。
6. v1.1.0 安装包桌面端实测结果（影响 0.5 是否可降级）。

---

*本规划仅为路线建议，遵循"先查后改、独立模块、字段前瞻性"原则；每个 Phase 动手前请按"待确认"核对现状。*
