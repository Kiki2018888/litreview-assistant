# 开发进度规划 2.1 — 现状 → SPEC v1.3.0 目标态

> 起点：当前基线（Phase 0 止血 + Phase 1 ADR-2 已部分落地）
> 终点：SPEC v1.3.0 §13 验收 + ADR-1~6 架构契约 + 第一条机会信号纵切片端到端跑通
> 对齐骨架：SPEC §11 Phase 0-5。配套差距分析见 `审阅报告2.md`（F1~F15）。
> 三条硬约束：①Phase 0 止血优先；②`claims` schema 是不可将就的地基，先于任何信号工作定稿落库；③纵切片节奏，首切片=局限聚类，借此在真实文献上验证 claims schema；**不把"打磨 prompt"列为里程碑**。

---

## 0. 总体判断：离终点约 5 个里程碑梯队

```
[已完成] 启动重置 · ADR-2 契约主体 · electron owner · 新字段产出/写库/响应
   │
   ▼
P0 止血收尾 ──→ P1 加固收尾 ──→ P2 Job化+并发(ADR-3/4) ──→ P3 结构债+重定位
                                                              │
                              (claims 地基是这里之后唯一不可将就项)
                                                              ▼
                                          P5 机会信号纵切片(产品价值核心)
                                          claims → 局限聚类端到端 → 矩阵/裁决 → 立项导出 → 矛盾检测
   (P4 embedding轻量语义 / token_usage 作为 P5 的依赖与并行项穿插)
```

价值密度排序：**P5 才是 v1.3 的"含金量"**；P0-P3 是让地基不塌的前置。P4 的 embedding 是 P5 召回的依赖，需在首切片召回前就位。

---

## Phase 0 · 止血收尾（最先做，便宜必做）

> 启动重置已完成；本阶段清剩余止血项 + ADR-2 那个关系契约成立与否的小漏洞。

| M | 目标 | 依赖 | 交付物 | 验收标准 | 粒度 | 归属 |
|---|------|------|--------|----------|------|------|
| M0.1 | 修 ADR-2 整字段省略绕过 | — | `schemas.py` `ExtractedDataContract` key_data/limitations 设为必填非空 | 单测：省略 key_data/limitations 的 JSON 被拒、不置 completed | 0.5d | 后端 |
| M0.2 | 修测试 URL + 冻结契约 | — | 清理/改写 `test_batches.py`；`docs/API_CONTRACT.md` 更新到 projects 现状 | pytest 全绿无 skip 债；契约与现路由一致 | 0.5d | 后端 |
| M0.3 | v1.1.x 安装包端到端回归 | M0.1-2 | 回归记录（项目/上传/删除/提取/设置） | 桌面端跑通主流程无 P0 | 0.5d | 全栈 |

**Phase 0 出口**：契约真生效（省略也拦）、测试无债、安装包可用。

---

## Phase 1 · 加固收尾（闭合新字段断链）

| M | 目标 | 依赖 | 交付物 | 验收标准 | 粒度 | 归属 |
|---|------|------|--------|----------|------|------|
| M1.1 | 问答消费层切到新字段 | M0.1 | `literature_chat.py` `_get_abstracts/_format_abstract_for_prompt` 读 research_question/sample_source/sample_size/key_methods/key_data/limitations | 跨文献问答上下文含新字段；旧字段不再被读 | 0.5d | 后端 |
| M1.2 | 自由问答 Prompt 轻量对齐（**非里程碑级打磨**） | M1.1 | 跨文献 Prompt 引用新字段；**不堆复杂矩阵 Prompt**（矩阵交给 P5） | 输出引用样本量/关键数据；改动小 | 0.5d | 后端 |

**Phase 1 出口**：新字段"产出→写库→响应→消费"全链路闭合，旧列只剩 FTS 依赖（待 P3 退场）。

---

## Phase 2 · 批量 Job 化 + 并发基线（ADR-3 + ADR-4）

> ADR-4 的 WAL 一行收益最高，建议在 Phase 0/1 期间就并行落地。

| M | 目标 | 依赖 | 交付物 | 验收标准 | 粒度 | 归属 |
|---|------|------|--------|----------|------|------|
| M2.1 | SQLite WAL + busy_timeout | — | `db.py` 每连接 PRAGMA `journal_mode=WAL`/`busy_timeout=5000` | 批量提取同时浏览无 `database is locked` | 0.5d | 后端 |
| M2.2 | extract_jobs 表 + 迁移 4 | M2.1 | 迁移 4；`tables.py` ExtractJob | 表+索引建成；迁移可升可降 | 0.5d | 迁移 |
| M2.3 | 后台 worker + `/jobs` 端点 | M2.2 | `services/extract_job_worker.py`、`api/v1/jobs.py`（列表/详情/stream/pause/resume/cancel）；`batch-extract` 改返回 `{job_id,total}` | 关页/断网 Job 继续推进；`GET /jobs/{id}` 进度对齐 | 2d | 后端 |
| M2.4 | 启动重置 job + SSE 订阅化 | M2.3 | lifespan 补 `running→interrupted`；SSE 退化为只读订阅；前端 useSSE Job 订阅重连 | 杀进程重启 job 标 interrupted 可恢复；订阅断开不影响任务 | 1d | 全栈 |
| M2.5 | async DB 访问统一 | M2.1 | 提取/问答/批量端点经线程池调 sync DB（或全 async） | async 端点不再直跑 sync DB（ADR-4） | 1d | 后端 |

**Phase 2 出口**：ADR-3、ADR-4 验收通过。

---

## Phase 3 · 结构债 + 立项重定位

| M | 目标 | 依赖 | 交付物 | 验收标准 | 粒度 | 归属 |
|---|------|------|--------|----------|------|------|
| M3.1 | paper_blocks 加 project_id（迁移 5） | — | 迁移 5；`UNIQUE(project_id, block_name)`；`paper.py`+`PaperWrite.tsx` 带 project_id | 多项目各自独立草稿 | 1d | 迁移+全栈 |
| M3.2 | chat_sessions 加 project_id | — | 迁移；会话可归项目 | 会话带项目归属 | 0.5d | 迁移+后端 |
| M3.3 | "论文撰写"→"立项材料导出"骨架重定位 | M3.1 | 模块更名/路由按 §4.3；分块编辑器作为 proposal 草稿载体（输入来源待 P5 接证据链） | 入口与文案改为立项导向；不破坏现分块编辑 | 1d | 全栈 |
| M3.4 | extracted_data 旧列退场（收尾迁移） | M1.1 | 全库回填后 DROP background/methods/key_results；改 FTS5 列 | 旧列移除后全链路无报错 | 1d | 迁移+后端 |

**Phase 3 出口**：结构债清零，立项模块骨架就位（待 P5 接入证据链）。

---

## Phase 4 · 能力增强（P5 的依赖 + 独立并行项）

| M | 目标 | 依赖 | 交付物 | 验收标准 | 粒度 | 归属 |
|---|------|------|--------|----------|------|------|
| M4.1 | 本地 embedding 轻量语义搜索/聚类（**非 RAG**） | — | 本地 embedding 服务，对摘要/论断做相似度；服务矩阵聚类与配对召回 | 能对论断/摘要算相似度；**不引入向量库 RAG 问答管线**（ADR-1） | 1.5d | 后端 |
| M4.2 | token_usage（ADR-5） | — | 迁移 6；各调用点记录；项目维度汇总端点 | 提取/问答后有记录，可汇总成本 | 1d | 迁移+后端 |

> M4.1 是 P5 局限聚类"确定性召回"的依赖，须在首切片召回步骤前就位；M4.2 可任意时点独立并行。

---

## Phase 5 · 机会信号（产品核心价值，纵切片驱动）

> **铁律**：claims schema 先定稿落库（不可将就）；然后以"局限聚类"一条纵切片端到端打通，借真实文献验证 schema；矩阵与矛盾检测排其后。各信号内部流水线见独立《详细功能设计》，不在此规划展开实现细节。

| M | 目标 | 依赖 | 交付物 | 验收标准 | 粒度 | 归属 |
|---|------|------|--------|----------|------|------|
| **M5.1** | **claims schema 抠定 + 迁移 7（地基, 不可将就）** | P2/P3 稳定 | 迁移 7；`tables.py` Claims（claim_type/subject/factor/direction/magnitude/stat/context/page_number/`verbatim_quote`/confidence）；索引 paper_id/claim_type/subject；级联删除 | 表落库；字段评审通过；`verbatim_quote`+page_number 必填 | 1d | 迁移+后端 |
| M5.2 | 论断抽取（Stage 0，走 ADR-2 校验+回灌） | M5.1 | 抽取链路：一篇一篇 close-reading 抽 claims，写库带逐字原句/页码；存量 completed 文献回填 | 抽取结果每条可钉回原句+页码；通过 schema 校验才落库 | 2d | 后端 |
| M5.3 | 确定性召回（局限聚类） | M5.2, M4.1 | 在 limitation 类论断、同主题簇内做确定性+embedding 聚类召回（宽进，论断级配对无组合爆炸） | 召回为确定性穷举/聚类，不靠模型"愿不愿找全"；候选可浏览 | 1.5d | 后端 |
| M5.4 | 聚焦裁决 + 出处穿透 | M5.3 | 对每候选一次只看极小输入做边界判定；判断落到证据片段；可 hash 缓存 | 每条信号卡片带论断+逐字原句+页码+跳转 PDF | 1.5d | 后端 |
| M5.5 | 可追溯 UI：信号面板 + accept/reject 落库 | M5.4 | 前端信号面板（卡片可下钻）；接受/否决(带理由)/改判/批注，全部落库 | 裁决落库可追溯；低置信/被否决进"待复核"而非静默吞掉 | 2d | 前端 |
| M5.6 | 文献矩阵视图 | M5.2 | "论文 × 维度"可交互矩阵（AI 填格来自 claims/extracted_data），排序/筛选/空格高亮 | 矩阵可读、可下钻到论断 | 2d | 前端 |
| M5.7 | 立项材料导出（投影，非生成） | M5.5, M3.3 | 从已确认机会+批注+勾选论断（带出处）组装 proposal/PPT/可行性/数据表 | 导出内容=人的判断+证据链投影，含可追溯出处；非 AI 另起炉灶 | 2d | 全栈 |
| M5.8 | 第二切片：矛盾检测 | M5.1-5.5 复用 | 同架构做矛盾点信号（技术最难，见《详细功能设计》） | 矛盾卡片双向出处、可裁决落库 | 视设计另估 | 后端+前端 |

**Phase 5 出口（首切片）**：局限聚类从 claims 抽取 → 召回 → 裁决 → 可追溯 UI → 落库 → 喂立项导出，**端到端跑通一遍真实文献**。这一刻 claims schema 得到实证检验，产品核心价值首次成立。

---

## 终点定义（"做到哪算完，可转入慢慢优化"）

> 三组勾选全绿 = 抵达 v1.3 目标态，可转入打磨期。

### A. 六条 ADR 架构契约（§13）
- [ ] **ADR-1**：问"摘要未覆盖"细节时，系统调取相关少数几篇**整篇全文**作答；全程无向量切片/RAG；`paper_pages` 全文完整保留（含 F13 的"摘要锁定→整篇带入"能力）
- [ ] **ADR-2**：喂格式错乱/字段敷衍/**整字段省略**的样例，走修复循环且不置 completed
- [ ] **ADR-3**：批量提取关页/重连 Job 继续推进、进度对齐；杀进程重启残留 extracting 被重置且 Job 可恢复
- [ ] **ADR-4**：批量提取同时浏览/查询，无 `database is locked`；async 端点不直跑 sync DB
- [ ] **ADR-5**：提取/问答后 token_usage 有记录，项目维度可汇总成本
- [ ] **ADR-6**：局限聚类由确定性召回+聚焦裁决产出，每条卡片可钉回逐字原句与页码；低置信/被否决进"待复核"而非静默吞掉；论断池可浏览

### B. §13 功能验收（节选关键项）
- [ ] 项目管理：3 项目、移动 paper_count 正确、删项目文献移入未分类
- [ ] 批量上传 30 篇全部解析；提取完成含 sample_size/key_data/limitations
- [ ] 单篇精读：PDF 预览 + 连续对话 + 页码定位
- [ ] 多 Provider：Moonshot/DeepSeek 测试连接 + 切换填充
- [ ] 立项材料导出：五分块输入/预览/自动保存；从已确认判断+证据链投影
- [ ] 打包 .exe 双击启动；API Key 加密持久化；更新检测无 404；无孤儿进程

### C. 第一条纵切片端到端（产品价值锚点）
- [ ] `claims` schema 定稿落库且经真实文献抽取检验（含 verbatim_quote/page_number）
- [ ] 局限聚类：claims 抽取 → 确定性召回 → 聚焦裁决 → 可追溯 UI(accept/reject) → 落库 → 喂立项导出，**整链跑通**
- [ ] 矩阵视图 + 信号面板可浏览、可下钻、可裁决

**达成 A+B+C 即为"终点"**：核心架构契约成立、主功能验收通过、产品含金量（首信号纵切片）端到端落地。此后矛盾检测(M5.8)、稀疏格/方法迁移、Prompt/UX 打磨均属"慢慢优化"，不阻塞"完成"判定。

---

## 并行/串行提示
- **可最先并行**：M2.1（WAL，一行高收益）、M4.2（token_usage 纯加表）、M0.1/M0.2（止血）。
- **强制串行**：M5.1 claims schema → M5.2 抽取 → M5.3 召回 →…（地基不先定稿，后续返工极贵）；M1.1 → M3.4（消费层切新字段后才能 DROP 旧列）。
- **首攻建议**：M2.1 + M0.1 当天可清；随后并行推进 P2 Job 化主线与 P3 结构债，为 P5 腾出干净地基。
